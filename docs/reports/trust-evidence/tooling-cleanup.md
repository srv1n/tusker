# Tooling cleanup (scripts lane)

Scope: `scripts/` except `measure-agent-workflows.py`, its fixtures/self-checks,
and `with-validation-lock.sh`; plus `scripts/tests` for changed scripts,
narrow `.gitignore` cache hygiene, and this report. No Go/Swift/UI,
Makefile, docs/system, or managed `.tusker` edits.

## Removed

- `scripts/check-file-size.sh` (52 lines) and `scripts/file-size-allowlist.txt`.
  Caller evidence: zero references in `Makefile`, `.github/workflows/ci.yml`,
  `.github/workflows/release.yml`, `docs/system`, Go sources, or skills
  (`rg check-file-size|file-size-allowlist` returns no matches after deletion).
  Header cited `skills/tusker/references/ENGINEERING_DISCIPLINE.md` (does not
  exist) and a `docs/ai-contribution-policy.md` file-size section (absent;
  that doc contains no file-size/allowlist text). Advisory-only
  (always `exit 0`), never gated a build. `FILE_SIZE_LIMIT` was referenced
  only by the deleted script itself.
- `--prepare-dir` option and its `rm -rf -- "$candidate"` block in
  `scripts/release-validate.sh` (usage line updated; `prepare_dir`
  variable/branch removed). Caller evidence: no production caller
  (`Makefile`, `scripts/release-build.sh`, `scripts/release-verify-tag.sh`,
  `.github/workflows/release.yml` use only `--check-only`); the sole caller
  was its own test. `--print-dir` and `--check-only` retained.
- `scripts/__pycache__/measure-agent-workflows.cpython-312.pyc` (untracked
  generated bytecode). Added narrow `scripts/__pycache__/` ignore to
  `.gitignore` under local toolchains/build outputs.

## Tests

- `scripts/tests/test-release-validate.sh` (owned test for the changed
  script): stale-dir/`--prepare-dir` block removed; symlink-refusal and
  version-accept coverage kept.
- `sh -n scripts/release-validate.sh` and
  `sh -n scripts/tests/test-release-validate.sh`: PASS (syntax).
- `sh scripts/tests/test-release-validate.sh`: PASS (18 cases).
- `sh scripts/tests/test-release-provenance.sh`: PASS (untouched generator,
  regression check).
- `rg check-file-size|file-size-allowlist|prepare-dir`: no matches.
- Go builds/tests not run: currently HELD by root; no private lock used.

## Preserved live boundaries

- `release-build.sh`, `release-verify-tag.sh`, `generate-release-sbom.py`,
  `generate-release-provenance.py`, `install.sh`,
  `install-pinned-minisign.sh`: all have live callers in `Makefile`
  (`release-artifacts`, `tag-release`) or `.github/workflows/release.yml`
  (build + publish jobs); untouched.
- `package-code-review.sh` and `codebasezip.py`: no production callers outside
  their own tests/`codebasezip` Makefile target, but both are manual review /
  handoff entrypoints; per lane rule they are preserved, not deleted to hit
  a number. The macOS Finder `open` side effect in `codebasezip.py` was
  considered and left alone (live behavior, no disuse evidence).

## Remaining risks

- Other lanes hold dirty edits across `cmd/`, `internal/`, UI, and `Makefile`;
  whole-repo validation (`make check`, `release-test` glob) is still theirs.
- `release-test` Makefile glob runs `test-release-*.sh` but never
  `test-package-code-review.sh`; that orphan wiring is outside this lane.
