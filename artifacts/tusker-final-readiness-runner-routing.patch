diff --git a/cmd/tusker/automation_commands_test.go b/cmd/tusker/automation_commands_test.go
index f5ca671..be0131c 100644
--- a/cmd/tusker/automation_commands_test.go
+++ b/cmd/tusker/automation_commands_test.go
@@ -7,6 +7,19 @@ import (
 	"testing"
 )
 
+func TestResolveRunnerHonorsAgentNextOwner(t *testing.T) {
+	wf := defaultWorkflow()
+	wf.Agents.Default = string(RunnerCodexExec)
+	wf.Agents.Enabled = []string{string(RunnerCodexExec), "chatgpt-browser"}
+	note := Note{Data: map[string]any{
+		"id":         "APP-T-0001",
+		"next_owner": "agent:chatgpt-browser",
+	}}
+
+	assertEqual(t, "chatgpt-browser", resolveRunnerForNote(note, wf), "daemon runner resolution")
+	assertEqual(t, "chatgpt-browser", automationResolveRunner(note, wf), "automation runner resolution")
+}
+
 func TestAutomationExplainJSONReportsBlockers(t *testing.T) {
 	vault := automationTestVault(t)
 	mustRunPickupTest(t, Args{"vault": vault, "quiet": "true", "epic": "APP", "title": "Human held", "risk": "low", "priority": "p1", "v7": "true"}, newV7Task)
diff --git a/cmd/tusker/daemon.go b/cmd/tusker/daemon.go
index f11abc7..7abda32 100644
--- a/cmd/tusker/daemon.go
+++ b/cmd/tusker/daemon.go
@@ -1053,6 +1053,13 @@ func shouldDaemonPromoteCleanExitToReview(noteStatus string) bool {
 }
 
 func resolveRunnerForNote(note Note, wf Workflow) string {
+	nextOwner := strings.TrimSpace(stringField(note.Data, "next_owner"))
+	if strings.HasPrefix(nextOwner, "agent:") {
+		candidate := strings.TrimSpace(strings.TrimPrefix(nextOwner, "agent:"))
+		if containsString(wf.Agents.Enabled, candidate) {
+			return candidate
+		}
+	}
 	assignee := strings.TrimSpace(stringField(note.Data, "assignee"))
 	for _, candidate := range wf.Agents.Enabled {
 		if assignee == candidate {
