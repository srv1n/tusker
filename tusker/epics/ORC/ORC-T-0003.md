---
schema: "tusker.task/v5"
id: "ORC-T-0003"
title: "Render WORKFLOW.md body as strict prompt template"
type: "task"
kind: "feature"
epic: "ORC"
status: "done"
priority: "p0"
risk: "medium"
size: "m"
delegation: "execute"
ai_assistance: "heavy"
blocked_by:
  - "ORC-T-0001"
  - "ORC-T-0002"
blocks:
  - "ORC-T-0004"
  - "ORC-T-0005"
  - "ORC-T-0011"
created: "2026-04-28"
updated: "2026-05-08"
started: "2026-04-28"
review_requested_at: "2026-04-30"
completed: "2026-05-08"
verified_by: "codex"
verified_at: "2026-05-08T05:25:16Z"
closed_by: "codex"
closed_at: "2026-05-08T05:25:16Z"
tags:
  - "symphony"
  - "phase-1"
  - "prompt"
---

# ORC-T-0003 - Render WORKFLOW.md body as strict prompt template

## Intent

This V5 task records the executable contract for the work described below.

## Scope

Use the detailed scope, non-goals, and plan sections below as the governing scope.

## Acceptance contract

Use the acceptance criteria below as the finished-world contract.

## Code/system anchors

Use the code anchors and canon references below as the implementation map.

## Constraints

Use the constraints, rejected options, risk notes, and rollout notes below as limits on implementation.

## Deliverables

Deliver implementation artifacts, tests, docs updates, evidence, and review packet links recorded below.

## Verification log

- 2026-05-08 — codex — verified — Review pass: checked task claims against implementation, ran tusker validate, go test ./..., and go vet ./...; no blocking issues found for this task.
Verification history is recorded in the evidence and work log sections below.

## Problem

Tusker has a WORKFLOW.md body, but daemon dispatch currently uses a hard-coded generic prompt. That makes the body decorative and pushes too much project behavior into the global skill bundle. Symphony's biggest design lesson is that the workflow body is the product: it is the project-specific runbook the agent actually follows.

## Scope and non-goals

In scope:

- Render WORKFLOW.md body into the first-turn prompt.
- Use strict template semantics: unknown variables or filters fail the attempt.
- Expose stable context for project, workflow, note, attempt, turn, workspace, runtime, and trust profile.
- Preserve a useful default body for projects that do not customize yet.
- Record render failures as attempt failures, not daemon crashes.

Out of scope:

- Continuation turn implementation. That lands in ORC-T-0011.
- Optional tool/MCP extension bridge. That lands in ORC-T-0015 after supervisor and proof surfaces are ready.
- A public plugin system for custom template filters.

Non-goals:

- Make the skill bundle obsolete. The body composes global guidance; it does not replace all reusable docs.
- Allow silent missing variables.

## Acceptance criteria

- [x] renderAttemptPrompt renders the workflow body, not a fixed seven-line prompt.
- [x] The rendered prompt includes note identity, workspace, attempt, evidence expectations, and workflow body text.
- [x] Unknown template variables fail the attempt with a clear error and recorded artifact.
- [x] Generated WORKFLOW.md includes a real starter runbook, not placeholder prose.
- [x] Tests cover valid template, invalid variable, missing section, and context shape.

## Canon

Read in this order:

- Epic: [[ORC]]
- docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Workflow Contract
- docs/specs/02-workflow-contract.md - Required Body Sections and prompt rendering rules
- skill/SKILL.md - global Tusker agent norms to reference, not duplicate blindly

## Code anchors

- cmd/tusker/daemon.go - renderAttemptPrompt and dispatchRun
- cmd/tusker/workflow.go - loadWorkflow and defaultWorkflowMarkdown
- cmd/tusker/workflow_validate.go - body validation
- cmd/tusker/smoke_test.go - daemon workflow tests

## Plan

1. Pick the template engine and strict-mode behavior explicitly.
2. Define the render context as a small typed structure.
3. Replace hard-coded prompt rendering with template rendering.
4. Persist rendered prompt artifacts as before.
5. Update default WORKFLOW.md body with the project runbook skeleton.

## Considered and rejected

- **Use Go text/template without strict missing-key behavior** - Rejected unless strict failure can be configured reliably.
- **Keep the body as human docs and add a separate prompt file** - Rejected because it splits project behavior across two files.

## Decision

The workflow body is the first-turn prompt template. This makes project behavior versioned and reviewable.

## Verification plan

- Unit tests for render context and strict failures.
- Integration test: daemon dispatch writes a prompt artifact containing rendered note/workspace values.
- Docs test: generated workflow body contains all required sections.
- Manual inspection of one rendered prompt artifact from a daemon run.

## Evidence

No execution evidence yet. Attach implementation artifacts, test output, review packet links, screenshots or videos where required by risk and surface.

- Implemented strict WORKFLOW.md body rendering with project, vault, repo, workspace, workflow, note, and attempt variables.
- Unknown or malformed placeholders fail dispatch cleanly instead of silently degrading to a generic prompt.
- Added cmd/tusker/workflow_prompt_test.go coverage.
- Verification: go test ./... passed.

## Kill list

- Remove any tests, docs, or defaults that still imply the old unsafe/unenforced behavior.

## Rollout

Ship with a compatible default template. Projects can edit WORKFLOW.md in repo review like any other policy change.

---

## Workpad

Read first:

1. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md
2. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Workflow Contract
3. docs/specs/02-workflow-contract.md - Required Body Sections and prompt rendering rules
4. skill/SKILL.md - global Tusker agent norms to reference, not duplicate blindly

Do not change:

- Do not implement unrelated Symphony features.
- Do not move runtime state into markdown frontmatter.
- Do not weaken risk gates to make automation easier.

Implement in order:

1. Pick the template engine and strict-mode behavior explicitly.
2. Define the render context as a small typed structure.
3. Replace hard-coded prompt rendering with template rendering.
4. Persist rendered prompt artifacts as before.
5. Update default WORKFLOW.md body with the project runbook skeleton.

Primary code anchors:

- cmd/tusker/daemon.go - renderAttemptPrompt and dispatchRun
- cmd/tusker/workflow.go - loadWorkflow and defaultWorkflowMarkdown
- cmd/tusker/workflow_validate.go - body validation
- cmd/tusker/smoke_test.go - daemon workflow tests

Stop conditions:

- Stop if strict render failures kill the daemon process.
- Stop if generated prompt omits workspace path or note identity.

## Work log

- 2026-05-08 — codex — closed — Verified in review sweep; implementation and evidence are sufficient for done.
- 2026-04-28 - tusker - task created during Symphony alignment planning spike.
- 2026-04-28 - codex - filled design packet, dependencies, acceptance criteria, and verification requirements.

- 2026-04-28 - codex - made WORKFLOW.md body load-bearing as the daemon prompt template.
