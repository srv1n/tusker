---
schema: "tusker.task/v5"
id: "ORC-T-0011"
title: "Implement same-thread continuation loop"
type: "task"
kind: "feature"
epic: "ORC"
status: "review"
priority: "p1"
risk: "high"
size: "l"
delegation: "execute"
ai_assistance: "heavy"
ai_tools: []
blocked_by:
  - "ORC-T-0003"
  - "ORC-T-0007"
  - "ORC-T-0008"
  - "ORC-T-0010"
blocks:
  - "ORC-T-0013"
  - "ORC-T-0017"
created: "2026-04-28"
updated: "2026-04-29"
started: "2026-04-28"
review_requested_at: "2026-04-30"
tags:
  - "symphony"
  - "phase-3"
  - "continuation"
---

# ORC-T-0011 - Implement same-thread continuation loop

## Intent

This V5 task records the executable contract for the work described below.

## Scope

Use the detailed scope, non-goals, and plan sections below as the governing scope.

## Acceptance contract

Use the acceptance criteria below as the finished-world contract.

## Code/system anchors

Use the code anchors and canon references below as the implementation map.

## Constraints

Use the constraints, rejected options, risk notes, and rollout notes below as limits on implementation.

## Deliverables

Deliver implementation artifacts, tests, docs updates, evidence, and review packet links recorded below.

## Verification log

Verification history is recorded in the evidence and work log sections below.

## Problem

Tusker's current model is effectively one runner pass followed by review packet. Symphony's core worker unlock is same-thread continuation: keep the Codex thread warm, re-check tracker state after each turn, and continue on the same thread when the note is still active.

## Scope and non-goals

In scope:

- Add max-turns workflow policy.
- Run multiple turns inside one attempt on the same Codex thread.
- Use full rendered prompt for first turn and continuation guidance for later turns.
- Re-check note state after each turn before deciding to continue or hand off.
- Implement 1-second continuation retry after clean worker exit if needed.

Out of scope:

- Tool host.
- Risk-tier routing beyond existing trust policy.
- Review packet generator beyond whatever minimal summary is needed.

Non-goals:

- Re-render full prompt every turn.
- Increment work_revision for continuation.
- Treat each continuation turn as a separate attempt.

## Acceptance criteria

- [x] One attempt can contain multiple turns on the same session/thread ref.
- [x] First turn prompt is full workflow-rendered prompt.
- [x] Continuation turns use short continuation guidance only.
- [x] Runner reloads note status between turns and stops if the note is no longer active.
- [x] Max-turn exhaustion is surfaced in the raw runtime log and does not loop inside the same process.
- [x] Clean exits with still-active tracker state queue a 1-second continuation retry instead of forcing review.

## Canon

Read in this order:

- Epic: [[ORC]]
- docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Phase 3 and Runtime Model
- docs/specs/05-runner-and-session-protocol.md - Continuation Contract
- docs/specs/06-review-rework-retry.md - Successful Attempt Handoff

## Code anchors

- cmd/tusker/runner_codex_live.go - turnStart and turn/completed handling
- cmd/tusker/daemon.go - dispatchRun, reconcileRun, markNoteReadyForReview
- cmd/tusker/runtime_store.go - attempts and turns
- cmd/tusker/runner_live_test.go - fake app-server multi-turn fixtures

## Plan

1. Add max_turns to workflow and validation.
2. Create turn records around each Codex turn.
3. After turn completion, reload note and decide continue/stop/review.
4. Send continuation guidance on same thread when eligible.
5. Record terminal attempt outcome once, after final turn.

## Considered and rejected

- **New attempt for each continuation turn** - Rejected because it ruins retry and resume semantics.
- **Resume command for steady-state continuation** - Rejected because resume is recovery, not the normal loop.

## Decision

Continuation is the normal worker loop. Resume remains recovery after interruption or restart.

## Verification plan

- Fake app-server test with three completed turns and same thread id.
- Test note moves to blocked between turns and loop stops.
- Test max_turns exhaustion classification.
- Test review packet after final successful turn.
- Manual run inspect shows one attempt with multiple turns.

## Knowledge delta

| Topic | Before | After | Audience | Target doc nodes |
|---|---|---|---|---|
| Task outcome | Operators lacked a checked statement of the task-facing behavior. | The task records the durable behavior, verification, and evidence expected after this work. | developer | none |

## Evidence

- 2026-04-28 - codex - implemented same-thread Codex continuation in `cmd/tusker/runner_codex_live.go`: first turn uses the rendered WORKFLOW prompt, later turns use short continuation guidance on the same thread.
- 2026-04-28 - codex - added daemon-side clean-exit safety in `cmd/tusker/daemon.go`: if the note is still in an active tracker state after a zero exit, the daemon queues a 1-second continuation retry and does not flip the note to review.
- 2026-04-28 - codex - added coverage in `cmd/tusker/runner_live_test.go` and `cmd/tusker/daemon_test.go` for multi-turn same-thread execution, continuation prompt shape, max-turn surfacing, and clean-active-exit retry.
- 2026-04-28 - codex - targeted verification passed: `go test ./cmd/tusker -run 'TestCodexLiveRunnerContinuesSameThreadWhileNoteRemainsActive|TestDaemonQueuesContinuationRetryWhenCleanExitLeavesNoteActive' -count=1`.

## Kill list

- Remove any tests, docs, or defaults that still imply the old unsafe/unenforced behavior.

## Rollout

Default max_turns should be conservative. Projects can raise it after observability proves stable.

---

## Workpad

Read first:

1. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md
2. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Phase 3 and Runtime Model
3. docs/specs/05-runner-and-session-protocol.md - Continuation Contract
4. docs/specs/06-review-rework-retry.md - Successful Attempt Handoff

Do not change:

- Do not implement unrelated Symphony features.
- Do not move runtime state into markdown frontmatter.
- Do not weaken risk gates to make automation easier.

Implement in order:

1. Add max_turns to workflow and validation.
2. Create turn records around each Codex turn.
3. After turn completion, reload note and decide continue/stop/review.
4. Send continuation guidance on same thread when eligible.
5. Record terminal attempt outcome once, after final turn.

Primary code anchors:

- cmd/tusker/runner_codex_live.go - turnStart and turn/completed handling
- cmd/tusker/daemon.go - dispatchRun, reconcileRun, markNoteReadyForReview
- cmd/tusker/runtime_store.go - attempts and turns
- cmd/tusker/runner_live_test.go - fake app-server multi-turn fixtures

Stop conditions:

- Stop if event normalization cannot track per-turn status.

## Work log

- 2026-04-28 - tusker - task created during Symphony alignment planning spike.
- 2026-04-28 - codex - filled design packet, dependencies, acceptance criteria, and verification requirements.
- 2026-04-28 - codex - implemented same-thread continuation loop and clean-exit continuation retry; moved to review for verifier pass.
