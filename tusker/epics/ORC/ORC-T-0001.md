---
schema: "tusker.task/v5"
id: "ORC-T-0001"
title: "Enforce workspace cwd and containment for daemon runners"
type: "task"
kind: "security"
epic: "ORC"
status: "review"
priority: "p0"
risk: "high"
size: "m"
delegation: "execute"
ai_assistance: "heavy"
ai_tools: []
blocked_by: []
blocks:
  - "ORC-T-0002"
  - "ORC-T-0003"
  - "ORC-T-0006"
  - "ORC-T-0007"
created: "2026-04-28"
updated: "2026-04-29"
started: "2026-04-28"
review_requested_at: "2026-04-30"
completed: "2026-04-28"
tags:
  - "symphony"
  - "phase-1"
  - "isolation"
---

# ORC-T-0001 - Enforce workspace cwd and containment for daemon runners

## Intent

This V5 task records the executable contract for the work described below.

## Scope

Use the detailed scope, non-goals, and plan sections below as the governing scope.

## Acceptance contract

Use the acceptance criteria below as the finished-world contract.

## Code/system anchors

Use the code anchors and canon references below as the implementation map.

## Constraints

Use the constraints, rejected options, risk notes, and rollout notes below as limits on implementation.

## Deliverables

Deliver implementation artifacts, tests, docs updates, evidence, and review packet links recorded below.

## Verification log

Verification history is recorded in the evidence and work log sections below.

## Problem

Tusker currently creates per-record workspaces but the live Codex runner can still start with the registration repo root as cwd. That collapses the isolation claim: two daemon runs can operate in the same checkout, and a user reading the workflow will believe the safer workspace path is being enforced when it is not.

This is not polish. It is the first honesty fix. No later orchestration work should build on a runner that can silently ignore the workspace it just prepared.

## Scope and non-goals

In scope:

- Make daemon-launched Codex and detached runner commands execute with cwd equal to the prepared workspace path.
- Keep repo root available as metadata through env vars and prompt/template context.
- Validate workspace path containment before launch.
- Reject or fail attempts when workspace metadata does not match project_id and record_id.
- Add regression tests that prove the runner cannot start in repo root.

Out of scope:

- Implementing continuation turns.
- Changing workspace creation strategy semantics beyond what is needed for cwd correctness.
- Deleting existing workspaces automatically outside explicit cleanup rules.

Non-goals:

- Hide the repo root from the agent. The repo root may be passed as read-only context or env metadata.
- Replace worktree/clone/copy strategies.

## Acceptance criteria

- [x] Codex app-server starts with thread cwd equal to the prepared workspace path, never the registration repo root.
- [x] Detached runner commands execute in the prepared workspace path.
- [x] Launch fails before process start if the workspace path is outside the daemon workspaces root.
- [x] Launch fails before process start if workspace metadata does not match project_id and record_id.
- [x] TUSKER_REPO_ROOT or equivalent metadata remains available for commands that need original repo context.
- [x] A regression test proves runner execution uses the workspace cwd and leaves repo-root metadata separate.

## Canon

Read in this order:

- Epic: [[ORC]]
- docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Workspace Rules and Phase 1
- docs/specs/04-workspace-manager.md - Safety Invariants
- docs/specs/05-runner-and-session-protocol.md - Start / Resume Contract

## Code anchors

- cmd/tusker/daemon.go - dispatchRun passes WorkingDir and WorkspacePath to runners
- cmd/tusker/runner_codex_live.go - startLiveCodex chooses thread cwd
- cmd/tusker/runner_exec.go - executeRunnerCommand chooses command cwd
- cmd/tusker/workspace_manager.go - workspace path and metadata
- cmd/tusker/workspace_manager_test.go or new workspace runner tests

## Plan

1. Add a single helper that resolves and validates runner cwd from WorkspacePath plus state-root containment.
2. Update Codex live and detached runner paths to use workspace cwd only.
3. Pass repo root separately through env/template context.
4. Add tests for cwd selection, path escape, metadata mismatch, and repo-root sentinel mutation.
5. Update docs if any command examples imply repo-root execution.

## Considered and rejected

- **Keep repo root as fallback cwd** - Rejected because fallback behavior is exactly how the isolation promise becomes false.
- **Only validate cwd in tests** - Rejected because runtime checks catch bad custom workflow and future adapter paths.

## Decision

Workspace cwd is mandatory for every daemon-run attempt. Repo root is metadata only. This makes some workflows slightly more explicit, but it makes isolation honest.

## Verification plan

- Unit test workspace containment helper with normal, escaped, empty, and symlink-like paths.
- Integration test daemon dispatch with fake runner that records cwd.
- Integration test live Codex command path using the existing app-server fake where practical.
- Manual test: dispatch one task and confirm pwd inside runner output is under the state-root workspaces directory.

## Knowledge delta

| Topic | Before | After | Audience | Target doc nodes |
|---|---|---|---|---|
| Task outcome | Operators lacked a checked statement of the task-facing behavior. | The task records the durable behavior, verification, and evidence expected after this work. | developer | none |

## Evidence

No execution evidence yet. Attach implementation artifacts, test output, review packet links, screenshots or videos where required by risk and surface.

- Implemented workspace cwd enforcement across live Codex, detached runner, and live Claude paths: cmd/tusker/runner.go, cmd/tusker/runner_codex_live.go, cmd/tusker/runner_exec.go, cmd/tusker/runner_claude_live.go.
- Added workspace metadata mismatch and root-containment checks in cmd/tusker/workspace_manager.go.
- Added regression coverage in cmd/tusker/runner_live_test.go and cmd/tusker/workspace_manager_test.go.
- Verification: go test ./..., go vet ./..., ./dist/tusker validate --vault ./tusker --json all passed on 2026-04-28.

## Kill list

- Remove any tests, docs, or defaults that still imply the old unsafe/unenforced behavior.

## Rollout

Ship behind the normal daemon path. If a project relied on repo-root cwd, it was relying on a broken isolation model; the fix is to use workflow hooks or repo-root metadata explicitly.

---

## Workpad

Read first:

1. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md
2. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Workspace Rules and Phase 1
3. docs/specs/04-workspace-manager.md - Safety Invariants
4. docs/specs/05-runner-and-session-protocol.md - Start / Resume Contract

Do not change:

- Do not implement unrelated Symphony features.
- Do not move runtime state into markdown frontmatter.
- Do not weaken risk gates to make automation easier.

Implement in order:

1. Add a single helper that resolves and validates runner cwd from WorkspacePath plus state-root containment.
2. Update Codex live and detached runner paths to use workspace cwd only.
3. Pass repo root separately through env/template context.
4. Add tests for cwd selection, path escape, metadata mismatch, and repo-root sentinel mutation.
5. Update docs if any command examples imply repo-root execution.

Primary code anchors:

- cmd/tusker/daemon.go - dispatchRun passes WorkingDir and WorkspacePath to runners
- cmd/tusker/runner_codex_live.go - startLiveCodex chooses thread cwd
- cmd/tusker/runner_exec.go - executeRunnerCommand chooses command cwd
- cmd/tusker/workspace_manager.go - workspace path and metadata
- cmd/tusker/workspace_manager_test.go or new workspace runner tests

Stop conditions:

- Do not start policy/template/continuation work until this is merged.
- Stop if any runner path still accepts repo root as cwd fallback.

## Work log

- 2026-04-28 - tusker - task created during Symphony alignment planning spike.
- 2026-04-28 - codex - filled design packet, dependencies, acceptance criteria, and verification requirements.

- 2026-04-28 - codex - implemented workspace cwd enforcement, repo-root env metadata, metadata mismatch checks, containment checks, and regression tests.
