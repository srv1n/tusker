---
schema: "tusker.task/v5"
id: "ORC-T-0009"
title: "Add sessions and run viewing commands"
type: "task"
kind: "feature"
epic: "ORC"
status: "blocked"
priority: "p1"
risk: "medium"
size: "m"
delegation: "execute"
ai_assistance: "heavy"
ai_tools: []
blocked_by:
  - "ORC-T-0007"
  - "ORC-T-0008"
block_reason: "blocked by unresolved dependency"
blocks:
  - "ORC-T-0013"
  - "ORC-T-0016"
created: "2026-04-28"
updated: "2026-04-30"
started: "2026-04-28"
blocked_since: "2026-04-30"
transitions:
  - at: "2026-04-30T07:14:16Z"
    kind: "status"
    from: "active"
    to: "blocked"
    actor: "tusker"
    reason: "blocked by unresolved dependency"
tags:
  - "symphony"
  - "phase-2"
  - "operator-ux"
---

# ORC-T-0009 - Add sessions and run viewing commands

## Intent

This V5 task records the executable contract for the work described below.

## Scope

Use the detailed scope, non-goals, and plan sections below as the governing scope.

## Acceptance contract

Use the acceptance criteria below as the finished-world contract.

## Code/system anchors

Use the code anchors and canon references below as the implementation map.

## Constraints

Use the constraints, rejected options, risk notes, and rollout notes below as limits on implementation.

## Deliverables

Deliver implementation artifacts, tests, docs updates, evidence, and review packet links recorded below.

## Verification log

Verification history is recorded in the evidence and work log sections below.

## Problem

Long-running orchestration is impossible to trust if operators cannot see what is alive, resumable, stalled, or abandoned. Tusker has runs inspect and logs today, but the view needs to understand attempts, turns, sessions, latest events, and resume availability.

## Scope and non-goals

In scope:

- Expand runs inspect to show attempts, turns, active turn, session ref, workspace, token totals, latest event, failure class, and resume status.
- Add runs events for structured event tails.
- Add sessions and sessions inspect commands or equivalent JSON output.
- Add Unix socket read operations for state, record, and refresh if low-cost.
- Keep Obsidian view requirements in docs.

Out of scope:

- Full web UI.
- Remote multi-user control plane.
- Tool host mutations.

Non-goals:

- Make raw logs the only operator surface.
- Expose unsafe write controls over the read API.

## Acceptance criteria

- [x] runs inspect presents attempt and turn summary in text and JSON. Active turn pointer remains open.
- [x] Operator can see latest session state/resumable data in inspect output. More state-specific fixtures remain open.
- [x] Structured event tail is available without reading raw logs manually.
- [x] JSON output includes turns, attempts, run, and latest session objects for CLI, Obsidian, and local read-view consumption.
- [ ] Tests cover inspect output for running, failed, abandoned, and completed attempts.

## Canon

Read in this order:

- Epic: [[ORC]]
- docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Resume And Viewing Semantics
- docs/specs/03-daemon-and-registry.md - Control Plane
- docs/specs/05-runner-and-session-protocol.md - Session

## Code anchors

- cmd/tusker/run_runtime_commands.go - runs inspect/logs/interrupt
- cmd/tusker/runtime_store.go - sessions and attempts queries
- cmd/tusker/daemon_control.go - Unix socket control path
- tusker/_system/views - future Bases outputs if needed

## Plan

1. Extend store queries to fetch attempts, turns, sessions, and event summaries.
2. Update runs inspect text and JSON formats.
3. Add runs events and session commands.
4. Add read-only daemon socket operations after CLI shape is stable.
5. Document output contracts in operator docs.

## Considered and rejected

- No high-risk alternatives recorded for this medium/low-risk task.

## Decision

Obsidian and CLI are the primary view layer. A local read API may support those surfaces; a web UI is not in this roadmap.

## Verification plan

- Unit tests for formatting and JSON shape.
- Integration test inspect on a fake running attempt.
- Integration test inspect on an abandoned resumable session.
- Manual check event tail and raw log tail both work.

## Evidence

No execution evidence yet. Attach implementation artifacts, test output, review packet links, screenshots or videos where required by risk and surface.

- runs inspect --json now includes turns.
- Text inspect summarizes turn count and latest turn status/tokens/last event.
- Added `runs events --id <ID> [--lines N] [--follow] [--json]` so operators can tail normalized JSONL events without opening raw runner logs.
- Added `TestRunsEventsTailsStructuredEventSink` coverage for parsed JSON event tail output.
- Still open: active turn pointer in inspect and abandoned/failed/completed matrix tests.

## Kill list

- None for planning phase.

## Rollout

Additive CLI/API surface. Keep old runs/logs commands working.

---

## Workpad

Read first:

1. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md
2. docs/specs/08-symphony-alignment-and-orchestration-roadmap.md - Resume And Viewing Semantics
3. docs/specs/03-daemon-and-registry.md - Control Plane
4. docs/specs/05-runner-and-session-protocol.md - Session

Do not change:

- Do not implement unrelated Symphony features.
- Do not move runtime state into markdown frontmatter.
- Do not weaken risk gates to make automation easier.

Implement in order:

1. Extend store queries to fetch attempts, turns, sessions, and event summaries.
2. Update runs inspect text and JSON formats.
3. Add runs events and session commands.
4. Add read-only daemon socket operations after CLI shape is stable.
5. Document output contracts in operator docs.

Primary code anchors:

- cmd/tusker/run_runtime_commands.go - runs inspect/logs/interrupt
- cmd/tusker/runtime_store.go - sessions and attempts queries
- cmd/tusker/daemon_control.go - Unix socket control path
- tusker/_system/views - future Bases outputs if needed

Stop conditions:

- Stop if output hides failure class or resume status.

## Work log

- 2026-04-30 — tusker — status: active → blocked — blocked by unresolved dependency
- 2026-04-28 - tusker - task created during Symphony alignment planning spike.
- 2026-04-28 - codex - filled design packet, dependencies, acceptance criteria, and verification requirements.

- 2026-04-28 - codex - added run inspection support for turns and session summaries.
- 2026-04-28 - codex - added structured event tail command for normalized run events.
