---
schema: "tusker.task/v7"
kind: "task"
id: "RUN-T-0016"
project: "tusker"
title: "Soft dependency edges: review-with-proof unblocks dispatch, close-order stays enforced"
epic: "RUN"
status: "ready"
readiness: "ready"
priority: "p1"
risk: "medium"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "RUN-T-0016"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-07T00:40:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-07T00:40:00Z"
updated_by: "agent:sarav"
state_rev: ""
---

# RUN-T-0016 · Soft dependency edges: review-with-proof unblocks dispatch, close-order stays enforced

## Intent

Operator friction (2026-07-06 night, feedback note `2026-07-06-sarav-soft-dependencies-allow-a-dependency-edge-to-unb.md`): the RUN-T-0010→0011 chain stalled overnight because dependency satisfaction requires the dependency to reach `done`, and `done` for high-risk tasks requires a human acceptor — so human review latency became **dispatch** latency for the whole chain, even though 0010 was machine-complete and gate-green hours earlier. Principle: **review gates closing, never flowing.**

Semantics:

1. Dependency edges gain a hardness: `hard` (today's behavior: dep must be `done`) and `soft` (dep must be in `review` with `proof_status: satisfied`, or `done`). Frontmatter form: `dependencies:` entries stay plain strings (default hardness applies) or become `"<TASK-ID>:soft"` / `"<TASK-ID>:hard"` — keep the parser backward-compatible with every existing task file.
2. Default hardness by the **dependency's** risk: deps on high/critical tasks default `hard`; deps on low/medium default `soft`. An explicit suffix always wins.
3. **Close-order is inviolable regardless of hardness**: a task cannot reach `done` while any dependency is not `done`. `tusker close` enforces this; the close error names the unfinished dependency.
4. Rework cascade: if a soft-unblocked dependency is rejected at review (moved to `rework`), every dependent that dispatched on the soft edge is flagged — running work gets the RUN-T-0011 heartbeat stop-signal path (its eligibility evaporates), non-started dependents flip back to `blocked_by_dependency`, and the review packet for the reworked task lists affected dependents so the human sees the blast radius.
5. `tusker automation plan` and the readiness reconciler both understand hardness; the daemon's shared eligibility function (RUN-T-0011) consumes readiness as today — no second eligibility site.

This task touches tracker/vault semantics (readiness, reconcile, close policy, plan), not the daemon dispatch core — safe to run in parallel with RUN-T-0011 by a second runner. If both land in the same wave, integration is via readiness fields only.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | A soft dependency in `review` with `proof_status: satisfied` unblocks the dependent's readiness; `backlog`/`ready`/`rework`/unsatisfied-proof review does not. Hard edges behave exactly as today. | Inline verification |
| A2 | Default hardness derives from the dependency's risk tier; explicit `:soft`/`:hard` suffix overrides; all existing task files parse unchanged. | Inline verification |
| A3 | Close-order enforced independently of hardness: closing a task with any non-done dependency fails with the dependency named. | Inline verification |
| A4 | Rework cascade: rejecting a soft-unblocked dependency re-blocks non-started dependents and stops running dependents via the eligibility path; the rework packet lists affected dependents. | Inline verification |
| A5 | Full suite green; reconcile over the existing vault produces no readiness changes for tasks without soft edges. | Inline verification |

## Non-goals

- No UI work (the readiness reason string flows through existing surfaces).
- No optimistic-merge/stacked-worktree mechanics (dependents build against main as today; code-level stacking is a separate decision).
- No change to review/close policy itself (who may accept what stays as-is).

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | go test ./cmd/tusker -run 'SoftDependencyUnblocks' -count=1 | pending | Review+satisfied unblocks; other states do not. |
| A2 | go test ./cmd/tusker -run 'DependencyHardnessDefaults\|DependencyParseCompat' -count=1 | pending | Risk-derived defaults; suffix override; back-compat. |
| A3 | go test ./cmd/tusker -run 'CloseOrderEnforced' -count=1 | pending | Close refuses with dependency named. |
| A4 | go test ./cmd/tusker -run 'SoftDependencyReworkCascade' -count=1 | pending | Re-block + stop-signal + packet lists dependents. |
| A5 | go test ./... -count=1 && go vet ./... | pending | Central gate green; vault reconcile stable. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

Expected: document the hardness rule in the workflow reference; the principle "review gates closing, never flowing" belongs in `.tusker/WORKFLOW.md`.
