---
schema: "tusker.task/v7"
kind: "task"
id: "RUN-T-0039"
project: "tusker"
title: "Close ceremony leaves live runtime rows: terminal tasks trip the invariant circuit and freeze fleet dispatch"
epic: "RUN"
status: "ready"
readiness: "ready"
priority: "p1"
risk: "high"
size: "s"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "RUN-T-0039"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-08T03:17:30Z"
created_by: "agent:sarav"
updated_at: "2026-07-08T03:22:39Z"
updated_by: "agent:sarav"
state_rev: "sha256:44770997df34ac0638da5a87bcb0aa02896222bc3585287eb3bd177d2a3baf95"
---

# RUN-T-0039 · Close ceremony leaves live runtime rows: terminal tasks trip the invariant circuit and freeze fleet dispatch

## Intent

Found in today's land-wave close ceremony (2026-07-08). After RUN-T-0034, OPS-T-0002, and SRV-T-0008 were closed via `tusker close`, the daemon.db runs table still held non-terminal rows for 13 tasks whose canonical status was already done/cancelled/backlog: CLN-T-0001, SRV-T-0007, RUN-T-0010/0011/0012/0013/0017/0026/0027/0034/0035/0036/0037, plus backlog rows SRV-T-0001 and CLN-T-0002. The daemon opened the invariant circuit — `invariant_violation: held lease task is not dispatch-eligible; repair stale terminal records with tusker runs retire` — and froze ALL fleet dispatch until an operator manually retired 15 rows and ran `daemon resume`. RUN-T-0037 built canonical-terminal retirement, but only on the RUNNER-observation path; operator `tusker close` and the reconcile tick never sweep runtime rows, so a clean operator close strands live leases behind terminal tasks and the circuit becomes routine hand-repair.

Semantics:

1. **Close authority.** Closing or cancelling a task retires its non-terminal runtime rows in the same operation — OR the daemon's reconcile tick sweeps any runtime row whose canonical task is terminal (done/cancelled/backlog). Pick one authority and make it the sole owner of the sweep; do not split it across both.
2. **Auditability.** Each retirement records who and why (e.g. `retired by close ceremony: canonical status done`), so a post-mortem can tell an operator-close retirement from a runner-observed one.
3. **Circuit stays last-resort.** The invariant circuit remains containment of last resort for any row the chosen authority somehow misses; it is not the mechanism operators must hand-repair on every close.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Fixture: a task closed via `tusker close` while a non-terminal runtime row holds its lease → the close retires the row in the same operation; no lease survives behind a terminal task. | Inline verification |
| A2 | Fixture: the reconcile tick observes a runtime row whose canonical task is terminal (done/cancelled/backlog) → the row is retired with an auditable who/why stamp. | Inline verification |
| A3 | Fixture: fleet dispatch stays open across a close ceremony that previously tripped the invariant circuit; the circuit fires only for rows the sweep could not reach. Existing park/redrive fixtures and full suite green. | Inline verification |

## Non-goals

- No change to park semantics or the redrive window (RUN-T-0023/0028 own those).
- No change to the invariant circuit's own trip/reset logic — only whether operators must hand-repair it.
- No change to canonical-terminal retirement on the runner-observation path (RUN-T-0037 owns that); this extends the same retirement to the operator/reconcile authority.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | TBD | pending | Operator `tusker close` retires held runtime rows in the same op. |
| A2 | TBD | pending | Reconcile-tick sweep of terminal-canonical rows + audit who/why stamp. |
| A3 | TBD | pending | Dispatch stays open across close ceremony; circuit last-resort only; full suite. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

Expected: CANON.md records that runtime rows are subordinate to canonical task state — a terminal task must own no non-terminal runtime row — and names the single sweep authority (close op or reconcile tick). WORKFLOW.md close ceremony gains the row-retirement step and documents the invariant circuit as last-resort containment, not routine operator repair.
