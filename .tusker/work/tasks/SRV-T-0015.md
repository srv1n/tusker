---
schema: "tusker.task/v7"
kind: "task"
id: "SRV-T-0015"
project: "tusker"
title: "Runs list is unreadable: NaN event timestamps, unlabeled columns, terminal churn rows masquerading as live runners"
epic: "SRV"
status: "backlog"
readiness: "ready"
priority: "p2"
risk: "medium"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "SRV-T-0015"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-08T03:17:37Z"
created_by: "agent:sarav"
updated_at: "2026-07-08T03:21:40Z"
updated_by: "tusker:reconcile"
state_rev: "sha256:4277d0217b7748d2a3ba9a52718195e2dab84a9659f8dc304f666c0f15894368"
---

# SRV-T-0015 · Runs list is unreadable: NaN event timestamps, unlabeled columns, terminal churn rows masquerading as live runners

## Intent

Observed today (2026-07-08) across the runs list and event tails (TRC-T-0002 and AGX-T-0005 pages). Three defects on the honest-runs surface:

- Every event-tail row renders `NaN:NaN:NaN` for its timestamp — a field-name/format mismatch between the runs API (reworked in the "honest runs API" wave) and the UI's timestamp parser.
- The runs list shows bare, unlabeled columns — e.g. `2.4`, `13:43.74`, `15`, `259` (apparently tokens-M, duration, attempts, events) — with no header row and no lease/outcome column.
- Dozens of pre-RUN-T-0036/0037 churn-era historical rows (14–15 attempts each) render visually identical to live runs, which is exactly why the operator read them as "numerous fake runners still running".

Semantics:

1. **Valid timestamps.** The event tail parses the timestamp field the runs API actually emits (matched name and format) and renders real clock times; no NaN.
2. **Labeled columns.** The runs list has a header row naming every column and surfaces lease_state and outcome per row.
3. **Terminal vs live distinct.** Terminal/historical rows are visually distinguished from live runs (styling/badge/section), so a settled 15-attempt churn row can never be mistaken for a running fleet member.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Fixture: an event carrying the API's real timestamp renders a valid clock time, never `NaN:NaN:NaN`. | Inline verification |
| A2 | Fixture: the runs list renders a labeled header row and a per-row lease_state/outcome column. | Inline verification |
| A3 | Fixture: a terminal churn row and a live run render visually distinct — a settled row cannot read as running. | Inline verification |

## Non-goals

- No change to the run detail header/token/elapsed logic (SRV-T-0014 owns that).
- No change to the runs API's recorded data model beyond aligning the emitted timestamp field the UI reads.
- No change to Retry/redrive controls (SRV-T-0016 owns those).

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | TBD | pending | Event-tail timestamp parses to a valid time fixture. |
| A2 | TBD | pending | Labeled columns + lease_state/outcome column fixture. |
| A3 | TBD | pending | Terminal vs live visual distinction fixture. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected. Serve UI rendering plus alignment to the runs API's emitted timestamp field; no CANON.md/WORKFLOW.md change.
