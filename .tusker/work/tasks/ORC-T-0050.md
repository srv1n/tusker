---
schema: "tusker.task/v7"
kind: "task"
id: "ORC-T-0050"
project: "tusker"
title: "Capture factory MVP first-run readiness"
epic: "ORC"
status: "backlog"
readiness: "held"
priority: "p1"
risk: "low"
size: "s"
wave: "W-0004"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "ORC-T-0050"
next_action: "Execute the imported delivery contract and satisfy proof mode."
domains:
  - "project"
spec_refs:
  - "docs/specs/19-factory-mvp-wave-one.md"
gates: []
dependencies: []
delivery_source_key: "capture-first-run-readiness"
delivery_plan_scope: "factory-mvp-wave-one/v1"
delivery_contract_fingerprint: "sha256:e8bac0d47e57f14a04a8e9738900c5d198c8d53364fd4fa174cc3147a79dbc47"
requirement_refs:
  - "R1"
  - "R2"
  - "R3"
artifact_contract:
  acceptance_ids:
    - "A1"
    - "A2"
    - "A3"
  kind: "document"
  path: "docs/reports/factory-mvp-first-run.md"
  summary: "Exact dry-run mapping, approved singleton enable/arm/start/dispatch observation, focused operations/E2E proof, read-only preflight state, and active-run/ref boundary record for first shadow/staging dogfood."
owned_paths:
  - "docs/reports/factory-mvp-first-run.md"
runner_profile: "implementation-terra"
concurrency_group: "factory-mvp-wave-one"
knowledge_nodes: []
created_at: "2026-07-26T10:15:37Z"
created_by: "agent:sarav"
updated_at: "2026-07-26T10:15:37Z"
updated_by: "agent:sarav"
state_rev: "sha256:4d388c5309761d8b8897f9c66f6ad846bfb6f0e96b4bc20685879805fba053e4"
---

# ORC-T-0050 · Capture factory MVP first-run readiness

## Intent

After separate explicit approval enables one project, arms `W-0004`, and starts the managed daemon, exactly this low-risk Terra-medium task writes the durable report from focused operations/E2E checks and read-only state, recording its existing configured bounded reviewer policy.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | The worker-owned report records passing focused `TestFactoryOperationsProjection` and `TestFactoryExecutionControl` command results against the current source revision from one configured Terra-medium implementation attempt, plus the configured bounded reviewer policy expected after worker submission. | See mapped verification. |
| A2 | The worker-owned report records the separately approved one-project enablement, `W-0004` arm, managed-daemon start, dispatch of only `ORC-T-0050`, and configured bounded reviewer policy, plus repository-built read-only setup/skill/validate/projects/factory/automation/daemon/service/wave surfaces. It captures preflight JSON and independently asserts `ok:true`; a known pre-existing red validate result is recorded as a blocker. Actual reviewer attempt/count/verdict/completion and post-run staging/ref observation belong only to a separate read-only coordinator check that does not modify this artifact. | See mapped verification. |
| A3 | Pre/post active-run and ref snapshots prove no unrelated claim/project, `all_eligible` widening, provider credential, release, default-ref movement, or extra implementation/triage/release/provider attempt occurred during the worker check window; the task refuses cleanly if `integration/W-0004` is absent and proves both it and `main` unchanged. A separate coordinator post-run check may later observe one normal staging advance. | See mapped verification. |

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | command: go test ./cmd/tusker -run '^TestFactoryOperationsProjection$' -count=1 && go test ./cmd/tusker -run '^TestFactoryExecutionControl$' -count=1 | pending |  |
| A2,A3 | command: sh -ceu '
dry=$(go run ./cmd/tusker delivery import --plan docs/plans/19-factory-mvp-wave-one-v2.yaml --dry-run --json)
printf "%s" "$dry" \| grep -F '"waveId":"W-0004"' >/dev/null
printf "%s" "$dry" \| grep -F '"capture-first-run-readiness":"ORC-T-0050"' >/dev/null
before_main=$(git rev-parse main)
before_integration=$(git rev-parse --verify integration/W-0004) \|\| { echo "missing integration/W-0004; refuse before report checks" >&2; exit 1; }
before_run=$(go run ./cmd/tusker runs inspect ORC-T-0050 --json)
before_queue=$(go run ./cmd/tusker automation queue --repo . --json)
go run ./cmd/tusker setup doctor --repo . --json
go run ./cmd/tusker skill doctor --strict --json
validate_rc=0; go run ./cmd/tusker validate --json \|\| validate_rc=$?
go run ./cmd/tusker projects list --json
go run ./cmd/tusker factory operations --json
go run ./cmd/tusker automation status --json
go run ./cmd/tusker daemon status --json
go run ./cmd/tusker daemon service status --json
go run ./cmd/tusker wave brief W-0004 --json
preflight=$(go run ./cmd/tusker wave preflight W-0004 --json)
printf "%s" "$preflight" \| grep -F '"ok":true' >/dev/null
after_run=$(go run ./cmd/tusker runs inspect ORC-T-0050 --json)
after_queue=$(go run ./cmd/tusker automation queue --repo . --json)
after_main=$(git rev-parse main)
after_integration=$(git rev-parse --verify integration/W-0004) \|\| { echo "integration/W-0004 disappeared during report checks" >&2; exit 1; }
test "$before_main" = "$after_main"
printf "%s\n%s" "$before_run" "$after_run" \| grep -F 'ORC-T-0050' >/dev/null
! printf "%s" "$before_queue" \| grep -F 'all_eligible' >/dev/null
! printf "%s" "$after_queue" \| grep -F 'all_eligible' >/dev/null
printf "%s" "$before_queue" \| grep -F '"effective":"armed_waves"' >/dev/null
printf "%s" "$after_queue" \| grep -F '"effective":"armed_waves"' >/dev/null
printf "%s" "$before_queue" \| grep -F '"active_runs":1' >/dev/null
printf "%s" "$after_queue" \| grep -F '"active_runs":1' >/dev/null
test "$before_integration" = "$after_integration"
test "$validate_rc" -eq 0 \|\| true
' | pending |  |

## Artifact contract

- Kind: `document`
- Path: `docs/reports/factory-mvp-first-run.md`
- Summary: Exact dry-run mapping, approved singleton enable/arm/start/dispatch observation, focused operations/E2E proof, read-only preflight state, and active-run/ref boundary record for first shadow/staging dogfood.
