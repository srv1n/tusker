---
schema: "tusker.task/v7"
kind: "task"
id: "TSK-T-0094"
project: "tusker"
title: "Make the Go suite and the e2e packages green, including crash recovery"
status: "backlog"
readiness: "blocked_by_dependency"
wave: "W-0044"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "blocked_dependency"
next_source: "dependency"
next_ref: "TSK-T-0086"
next_action: "Wait for dependency TSK-T-0086 to reach done."
dependencies:
  - "TSK-T-0086:hard"
  - "TSK-T-0087:hard"
  - "TSK-T-0088:hard"
  - "TSK-T-0089:hard"
  - "TSK-T-0090:hard"
  - "TSK-T-0091:hard"
  - "TSK-T-0092:hard"
  - "TSK-T-0093:hard"
contract_fingerprint: "sha256:2c4cbf18bce182f8b9119c82d0d0b5e02ca3f67e3d8190ee4c8dede1e6dd67ab"
owned_paths:
  - "cmd/tusker/capabilities_cmd.go"
  - "cmd/tusker/capabilities_cmd_test.go"
  - "e2e/crashrecovery"
  - "e2e/contractconvergence"
  - "e2e/executionobservability"
  - "internal/serve/ui/test"
  - "internal/serve/ui/tests"
work_level: "demanding"
created_at: "2026-09-23T16:45:01Z"
created_by: "agent:sarav"
updated_at: "2026-09-23T19:01:28Z"
updated_by: "agent:codex"
state_rev: "sha256:64faa53b4289fb5e028209482d293137e0bb73df6693e6ae8693ca32cf14b9b6"
authoring_provenance:
  binding_state: "unbound"
  captured_at: "2026-09-23T16:45:01.60115Z"
  conversation_id: "dd0c2668-a1a3-42ab-91b1-89b4df933521"
  host: "local"
  source: "claude"
---

## Intent
The gate is red independent of the session work: 26 cmd/tusker tests, e2e/contractconvergence and all of e2e/crashrecovery already fail at c5a5da52. Crash recovery is the only end-to-end proof that runs survive a daemon crash, and it fails at `tusker init` during setup. `tusker capabilities` omits commands added by these waves (`mcp`, `worker`, `message inbox`, `runs say`, `runs continue`, `work recover`, `projects automation-scope`). TestCurrentCheckoutUsesConfiguredProjectCapacity started failing in these waves. Five UI tests fail at baseline. Until the gate is green, no later change can be judged by it.

## Verified seams
- capabilities_cmd_test.go ~115/119 lists the missing commands.
- crash_recovery_test.go ~158 `tusker init --yes --vault ... --quiet` exits 1 after writing docs and skills.
- Many cmd/tusker fixtures fail with "contract drifted from its stored contract_fingerprint" (automation_capacity_test.go ~23/54/105, automation_commands_test.go ~124/244, daemon_lifecycle_test.go ~759/816).
- daemon_lifecycle_test.go ~478: agent_coordination.go enumerates project registrations instead of `loadRegisteredProjects`.
- browser_fixture_test.go ~16 requires TUSKER_BROWSER_FIXTURE_ROOT (should skip when unset).
- e2e/executionobservability passes on a clean checkout of 168c5c94 and fails with the dirty tree present; treat as environment-sensitive and make it independent of unrelated working-tree files.

## Implementation contract
Locked:
- Fix root causes, not tests, whenever production behavior is wrong. A test may change only when its expectation is obsolete; say which commit made it obsolete in the test comment.
- Fingerprint drift: find why the stored fingerprint no longer matches the fixture contract (fixture builder vs fingerprint function) and fix it at the source so every fixture is consistent.
- `tusker init` must succeed in a fresh temporary repository; the crash-recovery package must pass, including TestDaemonKillNineAdoptsSurvivingWrapper and TestDaemonRestartRedispatchesDeadWrapper.
- Browser fixture test skips with a message when its env var is unset.
- agent_coordination.go project enumeration uses `loadRegisteredProjects` (single-line change allowed in that file after mailbox-delivery lands).
- Never touch the operator's uncommitted files listed in shared_context. If a root cause is inside them (for example init output from the s46 docs work), stop and ask the operator with the evidence.
Proposed: the exact grouping of fixes; any production file beyond owned_paths must be named in the task log with the failing test it fixes.

## Failure and edge cases
- Full suite run twice in a row → identical pass result (no order or leftover-state dependence).
- Run with TUSKER_STATE_ROOT unset and set to a temp dir → both pass.

## Ownership and coordination
Runs after every code task in this wave so it can touch any file they owned. Hard-depends on all of them.

## Acceptance
| ID | Outcome | Proof |
| --- | --- | --- |
| A1 | `go test ./...` passes with zero failures on a clean checkout of the result. | V1 |
| A2 | Crash-recovery e2e passes, proving daemon kill -9 adoption and restart. | V1 |
| A3 | UI suite passes with zero failures. | V2 |

## Verification
| Covers | Check | Result | Notes |
| --- | --- | --- | --- |
| A1,A2 | command: rtk proxy go test ./... -count=1 -timeout 60m | pending | Read the exit code and FAIL lines, not a truncated log. |
| A3 | command: cd internal/serve/ui && bun run typecheck && bun test | pending | Zero failures. |
