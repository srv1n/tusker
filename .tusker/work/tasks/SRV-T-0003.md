---
schema: "tusker.task/v7"
kind: "task"
id: "SRV-T-0003"
project: "tusker"
title: "TipTap reading and editing layer for docs and specs"
epic: "SRV"
status: "backlog"
readiness: "blocked_by_dependency"
priority: "p2"
risk: "high"
size: "l"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "blocked_dependency"
next_source: "dependency"
next_ref: "SRV-T-0002"
next_action: "Wait for dependency SRV-T-0002 to reach done."
domains: []
gates: []
dependencies:
  - "SRV-T-0002"
evidence_required: []
created_at: "2026-07-05T11:32:32Z"
created_by: "agent:sarav"
updated_at: "2026-07-05T11:35:07Z"
updated_by: "agent:sarav"
state_rev: "sha256:b88ec94d7abc33d164f91c1245db43ae7d98343d577212bf71b7827cb8549c71"
---

# SRV-T-0003 · TipTap reading and editing layer for docs and specs

## Intent

Add the reading/editing layer to `tusker serve` per the phase-2 design approved in SRV-T-0001: a TipTap-based markdown editor for docs, specs, and task body sections, tuned for fast reading first (clean typography, rendered wikilinks, table support). Writes are the risky part and must be airtight: frontmatter state fields are never editable from the UI; body edits go through a write API that reuses the same mutation/validation paths as the CLI, with CAS on `state_rev` so concurrent agent writes conflict loudly instead of silently losing work; every save re-runs note validation and surfaces issues inline. Risk is high because this adds the first write surface to the web UI.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Docs, specs, and task bodies render in the TipTap reader with wikilinks and tables working. | Inline verification |
| A2 | Body edits persist through the CAS-guarded write API; a stale `state_rev` write is rejected with a visible conflict, not a silent overwrite. | Inline verification |
| A3 | Protected frontmatter/state fields are not editable from the UI, and attempts are rejected server-side as well. | Inline verification |
| A4 | Every save re-validates the note and shows warnings/errors inline; invalid saves do not corrupt the vault. | Inline verification |

## Non-goals

- No editing of evidence, attempts, events, or generated files.
- No collaborative/CRDT editing; single-operator local writes only.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | pnpm --dir <frontend dir> test -- editor | pending | Component tests for reader/editor rendering. |
| A2 | go test ./cmd/tusker -run 'ServeWriteCAS' -count=1 | pending | Stale-rev write rejected; fresh write lands. |
| A3 | go test ./cmd/tusker -run 'ServeProtectedFields' -count=1 | pending | Server-side rejection of state-field edits. |
| A4 | go test ./cmd/tusker -run 'ServeSaveValidate' -count=1 | pending | Validation runs on save; vault stays valid. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
