---
schema: "tusker.task/v7"
kind: "task"
id: "SRV-T-0003"
project: "tusker"
title: "Consolidate the project sidebar and add failed-run acknowledge"
epic: "SRV"
status: "backlog"
readiness: "blocked_by_dependency"
priority: "p2"
risk: "low"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "blocked_dependency"
next_source: "dependency"
next_ref: "SRV-T-0001"
next_action: "Wait for dependency SRV-T-0001 to reach review with satisfied proof or done."
domains: []
gates: []
dependencies:
  - "SRV-T-0001"
evidence_required: []
created_at: "2026-07-21T14:35:30Z"
created_by: "agent:sarav"
updated_at: "2026-07-21T14:38:08Z"
updated_by: "tusker:reconcile"
state_rev: "sha256:729a46bc226000514832e95a469bc00c2c0bd522c65e13ee7e36df21066ae296"
---

# SRV-T-0003 · Consolidate the project sidebar and add failed-run acknowledge

## Intent

- What this is: a slimmer sidebar and a way to dismiss dead alerts. Each
  project currently shows six navigation items; this folds the attention list
  and the run history into the project's Overview page, leaving four items:
  Overview, Work, Ops, and Docs alongside Library. On the Overview page,
  every failed-run alert gets an acknowledge action that clears it after the
  operator has decided it needs no further action.
- Why it matters: five projects times six items is a wall of navigation, and
  most items answer the same question — "what needs me and what happened?"
  Meanwhile a failed run today sits in the attention list forever unless the
  operator finds an obscure command-line incantation; an attention surface
  you cannot clear stops being trusted.
- What "done" looks like: the sidebar shows four items per project; Overview
  opens with attention items on top and recent runs below; clicking
  acknowledge on a failed run removes it from attention everywhere and it
  stays gone after a refresh.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | The project sidebar shows Overview, Work, Ops, Docs, and Library only; the Needs-me and Runs pages' content lives inside Overview (attention first, runs below). | Inline verification |
| A2 | Old links to the removed pages redirect to the right Overview section instead of breaking. | Inline verification |
| A3 | A failed-run card offers Acknowledge; it retires the run record through the existing mechanism and the card disappears from attention and stays gone. | Inline verification |
| A4 | The global "Needs me · all" view keeps working and reflects acknowledgements. | Inline verification |

## Non-goals

- No visual redesign beyond regrouping; keep the existing look.
- No change to how runs are recorded or what counts as needing attention.
- No removal of any data view — everything remains reachable via Overview.

## Implementation notes

Builder appendix — verify paths before editing.

- Frontend: `internal/serve/ui/` (React + TanStack Router + Tailwind v4,
  Bun-only). Sidebar component and project routes — grep for the route
  definitions of Needs me / Runs (ProjectNeeds, ProjectRuns pages) and fold
  their components into ProjectOverview as sections; add route redirects for
  the removed paths.
- Acknowledge: backend endpoint in `internal/serve/` wrapping the same path
  as `tusker runs retire <record> --reason <text>` (see cmd/tusker runs
  wiring); reason can default to "acknowledged from serve UI". Refuse for
  runs that are still active.
- Ordering with SRV-T-0001: this task depends on it only so the two do not
  fight over the sidebar and route files; the Docs item lands there.

## Verification

Each Check must start with `command: <exact shell command>` or `manual proof: <exact steps a human runs>`.

| Covers | Check | Result | Notes |
|---|---|---|---|
| A3 | command: go test ./internal/serve -run TestAcknowledgeRetiresFailedRun -count=1 | pending | |
| A3 | command: go test ./internal/serve -run TestAcknowledgeRefusesActiveRun -count=1 | pending | |
| A1 | command: cd internal/serve/ui && bun run build | pending | UI builds clean with regrouped routes |
| A1 | manual proof: open the tusker project; sidebar shows Overview, Work, Ops, Docs, Library; Overview shows attention items above recent runs | pending | |
| A2 | manual proof: visit the old /needs and /runs URLs for a project and land on the matching Overview section | pending | |
| A3 | manual proof: acknowledge one settled failed run; card disappears and stays gone after refresh | pending | |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
