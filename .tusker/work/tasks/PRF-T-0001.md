---
schema: "tusker.task/v7"
kind: "task"
id: "PRF-T-0001"
project: "tusker"
title: "Raise reconcile poll default and add interval override"
epic: "PRF"
status: "done"
readiness: "done"
priority: "p0"
risk: "low"
size: "s"
proof_mode: "inline"
proof_status: "satisfied"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
machine_status: ""
human_status: ""
closeout_status: ""
agent_action: ""
next_owner: "none"
next_source: "status"
next_ref: ""
next_action: ""
domains: []
gates: []
dependencies: []
evidence_required: []
accepted_by: "reviewer:independent"
accepted_at: "2026-07-15T06:03:19Z"
closed_at: "2026-07-15T06:03:19Z"
created_at: "2026-07-11T08:10:11Z"
created_by: "agent:sarav"
updated_at: "2026-07-15T06:03:19Z"
updated_by: "reviewer:independent"
state_rev: "sha256:255c6a2b9cbf04c52e326eaec231fdbde3eadb0606f658f686b25850e7220e75"
---

# PRF-T-0001 · Raise reconcile poll default and add interval override

## Intent

The daemon reconciles every 5 seconds because the workflow default stamps `runtime.poll_interval_ms = 5000` into every project (`cmd/tusker/workflow.go:148`). `nextPollInterval` (`cmd/tusker/daemon.go:203-228`) takes the **minimum** interval across all loadable registered projects, so one 5s project drags the whole daemon to 5s; the 30s fallback only applies when no project is loadable. There is no flag or env override — the legacy `poll.interval_seconds` config key (`cmd/tusker/config.go:54`) is display-only and dead.

Change the default to 30000ms (or 60000ms), add a global override (`TUSKER_POLL_INTERVAL_MS` env or daemon flag) with a sane floor (e.g. clamp ≥ 5000), and consider whether min-across-projects is even the right aggregation. Keep the watchdog consistent: stale threshold is `3 × pollInterval` (`cmd/tusker/daemon.go:46-50, 174-228`) — verify no false self-kill (`os.Exit(70)`) at longer intervals.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | With no project setting `poll_interval_ms`, the effective poll interval is ≥ 30s. | Inline verification |
| A2 | A global override (env or flag) beats per-project workflow values and is clamped to a floor. | Inline verification |
| A3 | Watchdog does not false-trigger at the longer default (staleness math tracks the effective interval). | Inline verification |

## Non-goals

- Incremental/mtime-gated scanning (PRF-T-0003).
- Event-driven architecture (PRF-D-0001).

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | command: go test ./cmd/tusker -run 'PollInterval' -count=1 | pass | Default workflow and daemon safety-net cadence are 60s; legacy project workflow values are not consulted. |
| A2 | command: go test ./cmd/tusker -run 'PollInterval' -count=1 | pass | TUSKER_POLL_INTERVAL_MS overrides the default, clamps below 5s, and rejects invalid or overflowing values. |
| A3 | command: go test ./cmd/tusker -run 'Watchdog' -count=1 | pass | Watchdog accepts exactly 3× the effective interval and rejects older beats. |
| A1,A2,A3 | review: poll interval override and adaptive watchdog integration in fdb29bd | pass | Independent review confirmed 60s default, explicit override with 5s floor, and watchdog cadence tracks adaptive safety intervals. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
