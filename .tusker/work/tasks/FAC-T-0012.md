---
schema: "tusker.task/v7"
kind: "task"
id: "FAC-T-0012"
project: "tusker"
title: "Replay a failed attempt for review without spending new model calls"
epic: "FAC"
status: "backlog"
readiness: "held"
priority: "p2"
risk: "low"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "FAC-T-0012"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
spec_refs:
  - ".tusker/specs/software-factory.md"
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-21T07:08:42Z"
created_by: "agent:sarav"
updated_at: "2026-07-21T07:08:42Z"
updated_by: "agent:sarav"
state_rev: "sha256:525147d5358417393605ef0df9bbf1922368b360aafd900268ec6686d56489d9"
---

# FAC-T-0012 · Replay a failed attempt for review without spending new model calls

## Intent

When an attempt fails and someone needs to judge what went wrong, they should be
able to replay that attempt from its recorded trail without spending any new
model calls. The replay reads what already happened; it does not go back and ask
the model again. That keeps adjudication cheap and honest — the reviewer sees the
real recorded steps, not a fresh, possibly different run.

Recordings of the run's edges and a replay already exist; this task guarantees
that a review replay makes zero fresh model calls and says so plainly, and that
it reports clearly when the recording is missing pieces instead of quietly
reaching for the model.

Done looks like: replaying a recorded failed attempt reproduces its steps for a
reviewer, reports that it made zero new model calls, and clearly flags an
incomplete recording rather than silently calling the model.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Replaying a recorded failed attempt reproduces its recorded steps for a reviewer. | Inline verification |
| A2 | The replay reports that it made zero new model calls. | Inline verification |
| A3 | An incomplete recording is reported clearly instead of silently calling the model. | Inline verification |

## Non-goals

- No re-execution against the live model under any flag in this adjudication mode.
- No repair of an incomplete recording; it is reported, not reconstructed.
- No new recording format; this reads the trail already captured.
- No UI surface for the replay here; it is a command-line adjudication path.

## Implementation notes

Best-guess file map — verify before editing:

- Boundary recording already exists: `recordRunBoundaryTraces`
  (`cmd/tusker/trace.go` around line 149). The replay exists too:
  `traceReplayCmd` and `ReplayTrace` (`cmd/tusker/trace_replay.go`, with a
  `ModelCalls` count field around line 61).
- Add or assert an adjudication path that guarantees zero new model calls,
  surfaces `ModelCalls == 0`, and returns a clear error when the trail is
  incomplete. Divergence is already handled via `TRACE_REPLAY_DIVERGED`; the
  incomplete-trail case needs the same clarity.
- Best guess: extend the existing mock / live-tools replay mode with a strict
  "adjudicate" mode that forbids model calls.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | command: go test ./cmd/tusker -run 'ReplayForAdjudicationReproducesSteps' -count=1 | pending | Recorded steps reproduced for review |
| A2 | command: go test ./cmd/tusker -run 'ReplayReportsZeroModelCalls' -count=1 | pending | Adjudication replay reports zero new model calls |
| A3 | command: go test ./cmd/tusker -run 'ReplayFlagsIncompleteTrace' -count=1 | pending | Incomplete recording reported, not silently modelled |
| all | command: make test | pending | Broad gate green |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
