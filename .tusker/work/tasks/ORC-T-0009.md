---
schema: "tusker.task/v7"
kind: "task"
id: "ORC-T-0009"
project: "tusker"
title: "Run gate-tier proof in harvest mode behind preflight refusal"
epic: "ORC"
status: "ready"
readiness: "ready"
priority: "p1"
risk: "medium"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "ORC-T-0009"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-20T07:04:56Z"
created_by: "agent:sarav"
updated_at: "2026-07-20T08:42:38Z"
updated_by: "tusker:reconcile"
state_rev: "sha256:4c98c3259456712e3251e4b69843caa65a180b8c0690f7df306d5f64cd3f6fbf"
---

# ORC-T-0009 · Run gate-tier proof in harvest mode behind preflight refusal

## Intent

On 2026-07-20 an integration lane burned roughly five hours discovering six
defects one at a time: the project's gate ran fail-fast, so each fix forced
another full compile/link cycle over ~199 test binaries. The project already
had a no-fail-fast runner profile; nothing selected it. Separately the same run
died at minute ~35 on a full disk, and the recovery deleted the build cache the
next run needed.

Make gate-tier execution structurally cheap: run gates through a Tusker-invoked
path that (1) performs cheap preflight checks and refuses early with a named
cause, (2) executes in harvest mode so one pass yields the COMPLETE failure
set, and (3) emits a structured defect list an agent can fix as one batch.
Preflight order per `skills/tusker/references/INTEGRATION_MERGE.md`: gate-ledger
hit, disk headroom, build-slot free, profile parity, tree frozen.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | A gate invoked through Tusker runs in the project's configured harvest (no-fail-fast) mode and returns every failure from one pass, not the first. | Inline verification |
| A2 | Preflight refuses before any expensive work when disk headroom is below the configured floor, the build slot is held, the profile does not match the canonical gate profile, or the tree is dirty — each refusal naming the cause and the remedy. | Inline verification |
| A3 | The run emits a structured defect list (per failure: command, target/test, first actionable diagnostic excerpt) consumable by a repair batch and by ORC-T-0004's repair-task creation. | Inline verification |
| A4 | A gate whose tree state already has a passing ledger entry (ORC-T-0005) short-circuits with a hit instead of re-running. | Inline verification |

## Non-goals

- No new test runner and no per-language runner logic beyond configuration:
  projects declare their harvest-mode command and preflight thresholds.
- No automatic fixing of defects; this task produces the list, not the patch.

## Verification

Each Check must start with `command: <exact shell command>` or `manual proof: <exact steps a human runs>`.

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | command: go test ./cmd/tusker -run 'GateHarvestMode' -count=1 | pass | Three commands, two failing: all three run, both defects returned. Second case proves a green command is ledgered for the next invocation. |
| A2 | command: go test ./cmd/tusker -run 'GatePreflightRefusal' -count=1 | pass | One subtest per precondition (disk, build slot, profile parity, frozen tree), each asserting cause, detail, remedy, and zero commands executed; plus doctrine-order and real-lock-file cases. |
| A3 | command: go test ./cmd/tusker -run 'GateDefectList' -count=1 | pass | Defect carries command, target, and first actionable excerpt; falls back to the command as target when no marker is configured; excerpt capped. |
| A4 | command: go test ./cmd/tusker -run 'GateLedgerShortCircuit' -count=1 | pass | Short-circuits on a full hit, runs only the missing commands on a partial hit, and is satisfied by the real ORC-T-0005 RuntimeStore ledger, not only a fake. |
| A1,A3 | command: tusker gate-run --vault ./.tusker --command "printf 'noise\n--- FAIL: TestReal\n  real_test.go:9: boom\n'; exit 1" | pass | Real binary on a real repo: `outcome=failed`, one `defects[]` entry carrying command, target, and the `--- FAIL: TestReal` excerpt. |
| A2 | command: touch scratch-dirty.txt && tusker gate-run --vault ./.tusker --command "true" | pass | `outcome=refused`, `cause=tree_not_frozen`, detail names `scratch-dirty.txt`, remedy names commit-or-stash; no command executed. |
| A4 | command: tusker gate-run --vault ./.tusker --command "true" (run twice) | pass | First run `outcome=passed` and ledgers the command; identical second run returns `outcome=ledger_hit` with `ledger_hits:["true"]` and an empty `ran` list. |
| A2 | command: tusker gate-run --vault ./.tusker | pass | Against this repo's own configured gate under real disk pressure: `outcome=refused`, `cause=disk_headroom`, detail "3.3 GB free ... below the configured 5.0 GB floor", in 182ms instead of a six-minute doomed run. The task's motivating scenario, reproduced. |
| A1,A3 | command: tusker gate-run --vault ./.tusker | pass | Same command with headroom available: runs the configured `make test` in harvest mode and returns per-test defects with target and excerpt harvested via the configured `^--- FAIL: (\S+)` marker. |
| all | command: make test | pass | Green at 8b9426f after the relative-symlink fix; `make vet` and `make fmt-check` also green. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
