---
schema: "tusker.task/v7"
kind: "task"
id: "TSK-T-0013"
project: "tusker"
title: "Reduce wave view to progress, capacity, action and DAG"
status: "backlog"
readiness: "ready"
wave: "W-0030"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "TSK-T-0013"
next_action: "Execute the task contract and satisfy proof mode."
contract_fingerprint: "sha256:0f247786b468857d84445768ad961be04deacffd964263d71933584053a93682"
owned_paths:
  - "internal/serve/ui/src/features/workbench/integration/WaveAuthority.tsx"
  - "internal/serve/ui/src/features/workbench/integration/integrationModel.ts"
  - "internal/serve/ui/src/features/workbench/integration/WorkExperience.tsx"
  - "internal/serve/ui/test/walkthrough-wave-overview.test.ts"
work_level: "standard"
created_at: "2026-09-17T14:32:57Z"
created_by: "agent:sarav"
updated_at: "2026-09-17T17:37:21Z"
updated_by: "tusker:reconcile"
state_rev: "sha256:c66662d1aeee2a8fadb56f69e637fd527b9c18d8e9d89363adcf2212dcd5d0d3"
authoring_provenance:
  binding_state: "unbound"
  captured_at: "2026-09-17T14:32:57.94562Z"
  conversation_id: "01a093ed-b11f-7432-8c8f-4d88d35740b8"
  host: "local"
  source: "codex"
---

## Intent
The wave header currently makes users read boilerplate and internal blocker codes before they can answer: what is happening, what is waiting, and do I need to act?

## Verified seams and ownership
`WaveAuthority.tsx` owns status prose, progress, controls and issue grouping. `integrationModel.ts` owns stage interpretation. The Dependencies DAG is already the first/default view from prior work; preserve it rather than rebuilding it.

## Implementation contract
Replace the large prose card with a compact glanceable summary: lifecycle badge, `completed / total`, counts for executing, reviewing, ready/waiting and failed, effective project capacity when it constrains work, and the one applicable primary action. Color and icon supplement text; never make color the only signal.

Delete filler such as `Work is prepared; nothing is running yet` and `This starts only the currently eligible tasks...`. Start, Pause and Resume may have one short contextual hint only when the behavior is not obvious. Normal dependency waits stay on the DAG node as `Waiting for <task title>`; they do not appear in an issue panel or raw Technical details. Material fingerprints and internal codes are hidden under a Technical disclosure.

A ready member blocked only by `maxActiveRunsPerProject=1` reads `Ready — waiting for an execution slot` and the wave shows `1 task at a time` with the existing settings destination, if available. Do not claim the scheduler is broken or offer parallel execution when the configured limit forbids it. When capacity is available, the UI may say `Ready — starting automatically` only after durable authorization exists.

Replace the generic fallback `Wave setup needs attention` with the specific task/lane failure and its supplied recovery action. Genuine route/configuration defects retain a setup message. Expected dependencies and capacity never fall through to that fallback.

## Acceptance
| ID | Outcome | Proof |
| --- | --- | --- |
| A1 | Ready, running, reviewing, paused, failed and completed waves expose compact counts and one correct primary action without boilerplate. | command: bun test --cwd internal/serve/ui test/walkthrough-wave-overview.test.ts |
| A2 | Expected dependency and capacity waits are neutral, named and visible on the DAG; raw fingerprints/codes are Technical-only. | command: bun test --cwd internal/serve/ui test/walkthrough-wave-overview.test.ts |
| A3 | A one-slot Beta fixture explains why only one sibling runs and updates when the slot is released; it never promises parallelism contrary to configuration. | command: bun test --cwd internal/serve/ui test/walkthrough-wave-overview.test.ts |
| A4 | Setup, implementation, verification and review failures remain distinct and show the machine or human actor responsible. | command: bun test --cwd internal/serve/ui test/walkthrough-wave-overview.test.ts |

## Verification
| Covers | Check | Result | Notes |
| --- | --- | --- | --- |
| A1,A2,A3,A4 | command: bun test --cwd internal/serve/ui test/walkthrough-wave-overview.test.ts | pending | Include accessible names and non-color status assertions. |
