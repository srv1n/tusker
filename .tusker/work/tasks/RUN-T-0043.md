---
schema: "tusker.task/v7"
kind: "task"
id: "RUN-T-0043"
project: "tusker"
title: "Daemon dispatch: claim runs via atomic ClaimRunLease CAS, not blind UpsertRun (prevent stale-claim overwrite of concurrent transitions)"
epic: "RUN"
status: "backlog"
readiness: "held"
priority: "p2"
risk: "medium"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "RUN-T-0043"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-08T12:05:35Z"
created_by: "agent:sarav"
updated_at: "2026-07-08T12:05:35Z"
updated_by: "agent:sarav"
state_rev: "sha256:ee820678436dea7a5983cce62636206bdccdb2da0f018d89a4d43e66fdd60e8c"
---

# RUN-T-0043 · Daemon dispatch: claim runs via atomic ClaimRunLease CAS, not blind UpsertRun (prevent stale-claim overwrite of concurrent transitions)

## Intent

From the GPT-5.5 Pro Extended pre-push review (handoff `cgpt_mrc05ks3_5fcda4b9`), MEDIUM finding on dispatch concurrency. `dispatchRun` (`cmd/tusker/daemon.go:2194`) claims work by mutating an in-memory `RunStatus` and blindly `UpsertRun`-ing it, even though `RuntimeStore` already exposes an atomic `ClaimRunLease` (`cmd/tusker/runtime_store.go:976`). The workspace prep, prompt rendering, `SaveAttempt`, and final upsert are **not** guarded by a compare-and-swap against the row read at poll start.

Consequence: a concurrent control action (stop/cancel/limit change from the CLI or serve UI) or a second `daemon`/`once` process can be silently overwritten by a stale dispatch claim — a lost update / lease race that can double-dispatch or clobber a just-changed runtime transition. Low probability in single-daemon operation, but it undermines the invariant that the active-run cap and lease state are authoritative.

Fix: move dispatch claiming to the atomic store operation (`ClaimRunLease`) with `owner`/`generation`/`work_revision` preconditions **before** any external side effects (workspace prep, spawn), and make every subsequent write in the dispatch path conditional on the same `lease_owner` + `lease_generation`. On a failed CAS, abort the dispatch cleanly (another actor won the row) without side effects.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Dispatch claims the run via an atomic CAS (`ClaimRunLease` with owner/generation/work_revision preconditions) before workspace prep or spawn; a lost CAS aborts dispatch with no side effects. | focused_test |
| A2 | A concurrent control mutation (e.g. cancel/limit) that changes the row between poll and claim is not overwritten by the stale dispatch; the dispatch backs off. | focused_test |
| A3 | Single-daemon happy path is unchanged (a run still dispatches normally when uncontended). | broad_test |

## Non-goals

- No change to the active-run capacity accounting or invariant-circuit logic beyond making the claim atomic.
- No multi-daemon coordination feature work; this only closes the existing single-store CAS gap.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1,A2 | `go test ./cmd/tusker -run 'Dispatch|Claim|Lease' -count=1` (add contended-claim + stale-overwrite cases) | pending | Define the smallest proof that proves acceptance. |
| A3 | `go build ./... && go vet ./... && go test ./... -count=1` | pending | Central gate. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
