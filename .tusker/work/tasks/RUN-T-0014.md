---
schema: "tusker.task/v7"
kind: "task"
id: "RUN-T-0014"
project: "tusker"
title: "Invariant sentinel: the daemon audits itself every tick and halts loudly on violation"
epic: "RUN"
status: "done"
readiness: "done"
priority: "p1"
risk: "medium"
size: "m"
proof_mode: "inline"
proof_status: "satisfied"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
machine_status: ""
human_status: ""
closeout_status: ""
agent_action: ""
next_owner: "none"
next_source: "status"
next_ref: ""
next_action: ""
domains: []
gates: []
dependencies:
  - "RUN-T-0011"
evidence_required: []
accepted_by: "human:sarav"
accepted_at: "2026-07-07T07:28:00Z"
closed_at: "2026-07-07T07:28:00Z"
created_at: "2026-07-07T00:30:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-07T07:28:00Z"
updated_by: "human:sarav"
state_rev: "sha256:7a1cccb8f2d713220df2687a251926039295a94205b7f849c26a1a415d81573a"
---

# RUN-T-0014 · Invariant sentinel: the daemon audits itself every tick and halts loudly on violation

## Intent

Binding design: `docs/design/prior-art-playbook.md` §1 (P1) and the fail-stop principle. Every incident so far was silent state corruption discovered by the operator, hours late, by looking at a UI. The sentinel inverts that: at the end of every reconcile tick (RUN-T-0011), the daemon checks a short list of invariants over its own runtime store; any violation means the reconciler itself is buggy — the correct response is fail-stop (halt new dispatch, keep serving reads, scream), never fail-silent.

Initial invariant set (extend as incidents occur — every future incident must add its invariant here or to the e2e suite):

1. No held lease whose task is not dispatch-eligible (the 88-attempt incident's signature).
2. No run whose attempt count exceeds the configured caps without a parked/terminal state.
3. No lease with a fresh heartbeat whose recorded `{pid, start_time}` fails liveness verification (heartbeats must not outlive their process).
4. No two active leases for one task; no lease owned by a daemon generation that is not the incumbent.
5. Spend accounted on active runs is monotonically non-decreasing across ticks (double-count/reset detection; budget layer arrives with RUN-T-0012 but the counter audit is independent).
6. `lastPollAt` advanced since the previous tick (the loop is actually looping).

On violation: dispatch circuit opens (same mechanism as the RUN-T-0012 budget circuit, distinct reason code `invariant_violation`), the violation with full detail is written to the runtime store and daemon log, `tusker daemon status` and the serve API report it first-line, and the serve UI shows a red banner. Recovery is explicit: `tusker daemon resume` after the operator (or a fix) clears the cause — the daemon never self-clears an invariant violation.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Each configured invariant, when violated by injected store state, is detected within one tick and opens the dispatch circuit with reason `invariant_violation` and violation detail. | Inline verification |
| A2 | While the circuit is open: no new dispatch, reads/serve unaffected, running work drains; status/API/UI all surface the violation honestly. | Inline verification |
| A3 | `tusker daemon resume` clears the circuit only when the violating state no longer exists; resume with the violation still present refuses loudly. | Inline verification |
| A4 | Sentinel overhead is bounded (single store pass per tick) and the check list is config-extensible without code changes for store-queryable predicates. | Inline verification |
| A5 | Full suite green; e2e scenario: inject a stale lease under a review task post-reconcile-bug simulation → circuit opens, daemon does not dispatch, resume-after-fix works. | Inline verification |

## Non-goals

- No auto-remediation: the sentinel detects reconciler bugs; the reconciler (RUN-T-0011) is the remediation for known states. Sentinel firing means unknown territory — that is precisely when automation must stop.
- No alerting integrations (mail/push); surfacing is status/API/UI only for now.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | go test ./cmd/tusker -run 'SentinelDetects' -count=1 | pass | 8 configured invariant detection cases passed; each violation opens invariant_violation circuit with detail |
| A2 | go test ./cmd/tusker -run 'SentinelCircuitOpen' -count=1 | pass | Circuit-open behavior blocks dispatch and surfaces status/API honestly |
| A3 | go test ./cmd/tusker -run 'SentinelResume' -count=1 && go test ./cmd/tusker -run 'TestRuntimeCommandParsingIncludesOperatorGroups' -count=1 | pass | Resume refuses while violation remains, clears after fix, and tusker daemon resume is routed |
| A4 | go test ./cmd/tusker -run 'SentinelBounded\|SentinelConfig\|SentinelAllowsCompletedRunnerStatus' -count=1 | pass | 3 tests passed for configured check list, one-pass supplied snapshot, and completed-status transient allowance |
| A5 | go test ./e2e/crashrecovery -run 'TestDaemonKillNineAdoptsSurvivingWrapper' -count=1 && go test ./... -count=1 && go vet ./... | pass | Crash-recovery adoption regression passed; full suite 382 tests across 7 packages; go vet no issues |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

Expected: the invariant list becomes the living definition of "the daemon is healthy"; record it in the playbook and require every future incident postmortem to extend it.
