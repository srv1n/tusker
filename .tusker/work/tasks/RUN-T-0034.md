---
schema: "tusker.task/v7"
kind: "task"
id: "RUN-T-0034"
project: "tusker"
title: "Audit wrapper direct-store exit classification: daemon-less exit 0 with tracker active must record early_exit"
epic: "RUN"
status: "review"
readiness: "waiting_on_review"
priority: "p2"
risk: "medium"
size: "s"
proof_mode: "inline"
proof_status: "satisfied"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "reviewer"
next_source: "review_policy"
next_ref: ""
next_action: "Review evidence and close or return to rework."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-07T13:30:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-07T17:11:16Z"
updated_by: "agent:sarav"
state_rev: "sha256:57f0fa4a28ccbcf59b89123cf9caca46bbca589cc9675ce55ce3ec0bbda39dcf"
---

# RUN-T-0034 · Audit wrapper direct-store exit classification: daemon-less exit 0 with tracker active must record early_exit

## Intent

Suspect finding from RUN-T-0017 trial run 3, step 2. During the daemon outage window (13:18:10–13:19:20Z), AGX-T-0005's wrapper exited on child exit and its attempt recorded `Outcome: succeeded, ExitCode: 0` — yet the task was still `ready` and the restarted daemon fenced and re-dispatched a fresh attempt. Under RUN-T-0030's semantics that attempt was an early exit (tracker state still active), not a success.

RUN-T-0030's classification fix lives on the daemon's status-observation path (`cmd/tusker/daemon.go`). The wrapper also records outcomes directly to the store when it outlives the daemon (RUN-T-0013/0018 direct-heartbeat path). Audit whether that path consults tracker state before classifying exit 0, and align it with the daemon path — one classification function, both writers.

The consequence today is bounded (the mislabeled attempt still consumed cap budget and re-dispatch was capped), but `succeeded` records that aren't successes poison the evidence trail and any future learning loop built on attempt outcomes.

Semantics:

1. One shared exit-classification function (per the WORKFLOW.md Runner Exit Classification table) used by every outcome writer: daemon status path AND wrapper direct-store path.
2. Wrapper recording an outcome daemon-less: exit 0 with tracker state still `ready`/`rework` records `early_exit`.
3. If the audit finds the wrapper cannot read tracker state cheaply at exit time, record `exit_unclassified` and let the daemon's adoption/reconcile pass classify it — never default to `succeeded`.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Audit finding in Evidence: where the wrapper path classifies exit 0, with file:line, and whether run-3's AGX-T-0005 attempt record is explained. | Inline verification |
| A2 | Fixture: wrapper outliving the daemon, child exits 0, tracker still active → attempt outcome is early_exit (or unclassified-then-reclassified), not succeeded. | Inline verification |
| A3 | Full suite green. | Inline verification |

## Non-goals

- No changes to wrapper stop conditions (fence advance / owner change / explicit stop / child exit).
- No retroactive rewriting of historical attempt records.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | manual audit: wrapper outcome-write path file:line trace | pass | Wrapper direct path now records after status at cmd/tusker/runner_wrapper.go:159 and classifies via cmd/tusker/runner_wrapper.go:200. Daemon uses the same classifier at cmd/tusker/daemon.go:1201. The shared classifier is cmd/tusker/runner_exit_classification.go:17; active tracker exit 0 returns early_exit at cmd/tusker/runner_exit_classification.go:37. AGX-T-0005 run-3 is explained by an exit-0 outcome path that bypassed tracker-aware daemon classification/defaulted to completion. |
| A2,focused_test | go test ./cmd/tusker -run 'WrapperDaemonlessExitClassification' -count=1 | pass | PASS: wrapper daemon-less child exit 0 with task still ready wrote attempt outcome early_exit, not succeeded. |
| A3,broad_test | go build ./... && go vet ./... && go test ./... -count=1 | pass | PASS: go build succeeded; go vet found no issues; full go test passed 443 tests in 7 packages. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

Expected: WORKFLOW.md Runner Exit Classification table gains a note that the classification applies identically on daemon and wrapper write paths.
