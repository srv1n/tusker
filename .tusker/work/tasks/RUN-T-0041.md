---
schema: "tusker.task/v7"
kind: "task"
id: "RUN-T-0041"
project: "tusker"
title: "Crashrecovery e2e suite leaks fixtures: fake-runner and wrapper processes orphan and accumulate forever"
epic: "RUN"
status: "done"
readiness: "done"
priority: "p2"
risk: "medium"
size: "s"
proof_mode: "inline"
proof_status: "satisfied"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
machine_status: ""
human_status: ""
closeout_status: ""
agent_action: ""
next_owner: "none"
next_source: "status"
next_ref: ""
next_action: ""
domains: []
gates: []
dependencies: []
evidence_required: []
accepted_by: "human:sarav"
accepted_at: "2026-07-08T05:10:07Z"
closed_at: "2026-07-08T05:10:07Z"
created_at: "2026-07-08T03:17:30Z"
created_by: "agent:sarav"
updated_at: "2026-07-08T05:10:07Z"
updated_by: "human:sarav"
state_rev: "sha256:ab2aa9358e75e1ab3c8526cab9d6ffacbde177b58139e5858e68731c2653f700"
---

# RUN-T-0041 · Crashrecovery e2e suite leaks fixtures: fake-runner and wrapper processes orphan and accumulate forever

## Intent

Found today (2026-07-08) during a routine backpressure gate. 624 live `fake-runner --mode hold` processes — plus their `tusker runner-wrapper` parents — were on the machine, all under `/var/folders/.../tusker-crash-e2e-bin-*/` and `/tmp/tusker-crash-daemon-restart-dead-wrapper-*/` state dirs, i.e. fixtures spawned by the `e2e/crashrecovery` tests. Every `go test ./...` (the standard backpressure gate every runner attempt runs) leaks more; they never exit, accumulate without bound, and alarmed the operator ("numerous fake runners"). Cleared today by hand with `pkill`. The tests deliberately exercise wrapper adoption / crash recovery, so the fixtures are legitimate — but they never reap their own children.

Semantics:

1. **Reap on exit.** Every crashrecovery fixture that spawns a runner-wrapper or fake-runner reaps its children on test exit — a defer / `t.Cleanup` that kills the whole process group, not just the direct child, so adopted/re-parented processes die too.
2. **Self-expiring holds.** Hold-mode fake runners carry a hard wall-clock timeout (bounded well above the longest test) so any orphan that escapes a cleanup self-exits instead of living forever.
3. **Suite-end assertion.** A suite-teardown check asserts no fixture process survives (by state-dir prefix / marker) and fails the suite if the reap missed any — so a future leak is caught by the gate, not by the operator noticing hundreds of processes.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Fixture: a crashrecovery test that spawns wrapper + fake-runner leaves zero surviving fixture processes after it returns (process-group reap covers adopted children). | Inline verification |
| A2 | Fixture: a hold-mode fake runner orphaned past its hard timeout self-exits without an external kill. | Inline verification |
| A3 | Suite-end check asserts no `tusker-crash-*` fixture process survives; running the crashrecovery suite twice back-to-back does not accumulate processes. | Inline verification |

## Non-goals

- No change to production wrapper adoption or crash-recovery semantics — the tests exercise those on purpose; only the fixtures' own child lifecycle changes.
- No change to the fake-runner hold-mode protocol beyond adding the safety timeout.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | go test ./e2e/crashrecovery -run 'FixtureProcessCleanup\|NoSurvivingFixtureProcesses' -count=1 | pass | Focused cleanup fixture passed; process-group reaper left zero marker-matched survivors. |
| A2 | go test ./e2e/crashrecovery -run 'HoldModeFakeRunnerSelfExpires' -count=1 | pass | Hold-mode fake runner exited 124 after its hard timeout without external kill. |
| A3 | go test ./e2e/crashrecovery -count=2 && go test ./... -count=1 && go vet ./... | pass | Crashrecovery passed twice with suite-end survivor assertion; broad test and vet passed serially. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

Expected: WORKFLOW.md test-hygiene note that e2e fixtures spawning processes must process-group-reap on cleanup and self-expire, with a suite-end no-survivor assertion. CANON.md unchanged.
