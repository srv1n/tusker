---
schema: "tusker.task/v7"
kind: "task"
id: "OPS-T-0004"
project: "tusker"
title: "Ralph worker protocol: fresh context per attempt, durable plan, backpressure"
epic: "OPS"
status: "ready"
readiness: "ready"
priority: "p1"
risk: "medium"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "OPS-T-0004"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-07T01:45:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-07T01:45:00Z"
updated_by: "agent:sarav"
state_rev: ""
---

# OPS-T-0004 · Ralph worker protocol: fresh context per attempt, durable plan, backpressure

## Intent

Binding design: `docs/design/prior-art-playbook.md` §5 decision 4 (donor: the Ralph loop — `docs/design/research/research-operating-models.md` §2). Two motivations, one of them expensive and already observed: **continuation attempts currently append to the same codex thread**, so per-attempt input grows linearly and total burn quadratically — the 2026-07-06 incident's 1.63B input tokens were mostly re-reads of one ever-growing thread. Ralph's core insight is that fresh context per unit of work is *better* as well as cheaper: the model works in its context-quality sweet spot, and durable state lives on disk where the next attempt (or a different runner) can pick it up.

Semantics:

1. **Fresh context per attempt:** every dispatched attempt starts a NEW runner session/thread (codex app-server: new conversation; claude: new session). No attempt ever appends to a predecessor's thread. The prompt carries: the task packet, the plan file (below), and the previous attempt's structured outcome (error/park reason) — never the raw predecessor transcript.
2. **Durable plan file per task:** `.tusker/scratch/<TASK-ID>/PLAN.md` — the attempt reads it first, maintains it (checklist of sub-steps with done/pending), and writes it before finishing or when the heartbeat stop-signal fires. It is disposable state, not evidence: never referenced from task markdown, cleaned when the task closes. The prompt template instructs: read plan → do the next undone item(s) → update plan → verify → finish.
3. **One-task-per-attempt discipline** stays absolute (already Tusker's shape); the prompt template's command budget gains the Ralph rules: search-before-implement (no duplicate implementations), no placeholder/stub code, serialized validation (one build/test invocation, not parallel storms).
4. **Backpressure formula from config:** the repo's gate formula (shared with OPS-T-0002's lane; `tusker.yaml`) is injected into the prompt as *the* verification commands, replacing free-text convention.
5. **Prompt-template "signs":** a per-repo `signs.md` (operator-maintained, injected into every attempt prompt) for accumulated corrective rules — the "tune it like a guitar" loop. Keep it capped (~60 lines) with a validation warning when it bloats.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Each attempt provably starts a fresh runner session (codex + claude runners); the dispatch prompt contains plan-file contents and prior structured outcome, never prior transcript. | Inline verification |
| A2 | Plan-file lifecycle: created on first attempt, read/updated by the template contract, survives between attempts, removed at close; never referenced from task markdown. | Inline verification |
| A3 | Prompt template carries the backpressure formula from config and the Ralph rules (search-before-implement, no placeholders, serialized validation); `signs.md` injected when present with a bloat warning past the cap. | Inline verification |
| A4 | Token regression guard: a simulated 5-continuation task shows per-attempt prompt input roughly flat (no cumulative thread growth) in the runner fixture. | Inline verification |
| A5 | Full suite green. | Inline verification |

## Non-goals

- No seance/predecessor-querying mechanics (structured outcome + plan file is the handoff; revisit only if it proves insufficient).
- No change to attempt/retry caps (RUN-T-0010/0011 own those).
- No per-model context-window tuning.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | go test ./cmd/tusker -run 'FreshSessionPerAttempt' -count=1 | pending | New conversation per attempt, both runners. |
| A2 | go test ./cmd/tusker -run 'PlanFileLifecycle' -count=1 | pending | Create/persist/clean; absent from task md. |
| A3 | go test ./cmd/tusker -run 'PromptBackpressureAndSigns' -count=1 | pending | Config formula injected; signs cap warning. |
| A4 | go test ./cmd/tusker -run 'AttemptInputFlat' -count=1 | pending | Fixture: no quadratic growth across 5 attempts. |
| A5 | go test ./... -count=1 && go vet ./... | pending | Central gate green. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

Expected: prompt-template changes documented; `signs.md` convention added to `.tusker/SKILL.md` routing so future operators know it exists and what belongs there.
