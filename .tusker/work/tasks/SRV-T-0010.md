---
schema: "tusker.task/v7"
kind: "task"
id: "SRV-T-0010"
project: "tusker"
title: "Unify the editor: click-to-edit prose in the task view, retire the raw textarea and the markdown detour"
epic: "SRV"
status: "ready"
readiness: "ready"
priority: "p1"
risk: "medium"
size: "m"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "SRV-T-0010"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-06T16:37:58Z"
created_by: "agent:sarav"
updated_at: "2026-07-07T11:15:58Z"
updated_by: "agent:sarav"
state_rev: "sha256:3ab3f953dcf2aff12c98eb5742ef576f4838b9dd369a6b346e0d5277b6003dd1"
---

# SRV-T-0010 · Unify the editor: click-to-edit prose in the task view, retire the raw textarea and the markdown detour

## Intent

Operator friction (2026-07-06): editing a task today is an awkward detour. You land on the structured task view (`TaskContract.tsx`), but real body editing lives on a *different route* — the "Open markdown" button (`TaskContract.tsx:69-77`) links to `DocReader`, which opens read-only, and only then does an "Edit" button (`DocReader.tsx:126`) flip the same TipTap surface to editable. The button says "markdown" but `DocReader` is not raw markdown — it is the same WYSIWYG TipTap render (`editor/DocEditor.tsx`, `editable` toggled), so the label actively misleads and makes editing feel like "drop into markdown first." Meanwhile TaskContract's own inline edit is a bare monospace `<textarea>` (`EditableProse`, `TaskContract.tsx:348-420`) — the "garbage" edit state — already flagged unfinished at `TaskContract.tsx:347`: `TODO(api): TipTap markdown surface (shared with the doc reader)`.

Fix — one surface, no ceremony. The good editor (`editor/DocEditor.tsx`, TipTap + tiptap-markdown, already built) becomes the prose surface *in the task view*:
- Replace `EditableProse` with `DocEditor`; delete the raw textarea path.
- Fold `DocReader`'s edit capability into TaskContract so there is no route hop to edit. The read view is already a TipTap instance with `editable=false`, so clicking into a prose block flips `editable` on — cursor in, type.
- Demote "Open markdown" to a read-only "View source" escape hatch (raw `.md`), not the path to editing. The task view becomes the single home for reading and editing.

Scope seam: this task delivers the unified click-to-edit surface and removes the detour and the textarea. It does **not** change the save model — editing still commits through the existing `useDocEditor` path (`editor.ts`) with CAS/validation intact, as an interim. SRV-T-0011 replaces that trigger with ambient autosave and removes any explicit save affordance. Sequencing this way keeps each task shippable and low-risk.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Prose sections in the task view (Intent and other body prose) render and edit in the same TipTap surface (`DocEditor`); the raw monospace textarea (`EditableProse`) is removed from the tree. | Inline verification |
| A2 | Editing prose requires no route change and no "Edit" mode button as the entry point — clicking into a prose block focuses it for editing in place on the task view. | Inline verification |
| A3 | "Open markdown" is renamed and demoted to a read-only "View source" affordance; the structured task view is the single surface for both reading and editing. | Inline verification |
| A4 | Persistence continues through the existing `useDocEditor` save path with CAS and validation intact (interim; SRV-T-0011 replaces the trigger). No data-loss regression versus today. | Inline verification |

## Non-goals

- No autosave here — the commit trigger stays as-is; SRV-T-0011 makes it ambient.
- No front-matter editing (SRV-T-0009 owns that).
- No new TipTap extensions or backend endpoints; reuse `editor/DocEditor.tsx` as built.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | command: bun --cwd internal/serve/ui test task-editor | pending | Prose edits via DocEditor; EditableProse gone. |
| A2 | command: bun --cwd internal/serve/ui test task-editor | pending | Click-into-block edits in place; no route hop, no entry Edit button. |
| A3 | command: bun --cwd internal/serve/ui test task-editor | pending | "View source" is read-only; task view hosts read + edit. |
| A4 | command: bun --cwd internal/serve/ui test docs-save | pending | Existing CAS/validation save still passes; no regression. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
