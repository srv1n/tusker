---
schema: "tusker.task/v7"
kind: "task"
id: "RUN-T-0029"
project: "tusker"
title: "Every project-loading entry point must quarantine broken registrations, not fatal"
epic: "RUN"
status: "ready"
readiness: "ready"
priority: "p1"
risk: "medium"
size: "s"
proof_mode: "inline"
proof_status: "pending"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
next_owner: "agent"
next_source: "task"
next_ref: "RUN-T-0029"
next_action: "Execute the task contract and satisfy proof mode."
domains: []
gates: []
dependencies: []
evidence_required: []
created_at: "2026-07-07T12:00:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-07T12:00:00Z"
updated_by: "agent:sarav"
state_rev: ""
---

# RUN-T-0029 · Every project-loading entry point must quarantine broken registrations, not fatal

## Intent

Found live during RUN-T-0017 trial run 2, step 6. RUN-T-0021 taught `tusker daemon run` to quarantine a broken project registration (missing `WORKFLOW.md`) and keep serving healthy projects. But `tusker daemon resume` still loads all registrations through the old fatal path: with the broken kurpod registration enabled, `resume` exits with `[NOT_FOUND] .../kurpod/tusker/WORKFLOW.md` instead of resuming the healthy project.

This is the same bug class fixed once, resurfacing on a second entry point — which means the quarantine behavior lives in the `daemon run` call site rather than in the loader itself.

Semantics:

1. One shared project-loading function owns quarantine semantics: a registration that fails to load is marked quarantined with the error recorded, and loading continues for the remaining registrations.
2. Every CLI and daemon entry point that enumerates or loads project registrations routes through that function — audit at minimum: `daemon run`, `daemon resume`, `daemon stop`, `daemon limits`, `runs` subcommands, `projects list`, `reconcile`, and serve handlers.
3. An entry point whose *target* project is the quarantined one may still error loudly — refusing to resume a project that cannot load is correct; dying because an unrelated registration is broken is the bug.
4. `projects list` surfaces quarantined state and the recorded error so the operator can see what to fix.

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | Fixture: two registrations, one with a missing WORKFLOW.md; `daemon resume` succeeds for the healthy project and reports the broken one as quarantined, exit 0. | Inline verification |
| A2 | Grep-provable: all registration-enumerating entry points call the single shared loader; no remaining direct fatal path on registration load error. | Inline verification |
| A3 | Full suite green. | Inline verification |

## Non-goals

- No auto-repair or auto-disable of broken registrations; quarantine + surface only.
- No change to RUN-T-0021's `daemon run` behavior beyond moving it into the shared loader.

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | go test ./cmd/tusker -run 'ResumeQuarantinesBrokenRegistration' -count=1 | pending | Resume survives an unrelated broken registration. |
| A2 | go test ./cmd/tusker -run 'SharedProjectLoaderAllEntryPoints' -count=1 | pending | One loader, every entry point. |
| A3 | go test ./... -count=1 && go vet ./... | pending | Central gate green. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

Expected: docs note that quarantine-on-load is a loader property, not a per-command behavior; RUN-T-0021's fix location updated to reference the shared loader.
