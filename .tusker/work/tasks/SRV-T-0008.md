---
schema: "tusker.task/v7"
kind: "task"
id: "SRV-T-0008"
project: "tusker"
title: "Live updates: SSE event stream from the daemon poll loop"
epic: "SRV"
status: "done"
readiness: "done"
priority: "p2"
risk: "low"
size: "m"
proof_mode: "inline"
proof_status: "satisfied"
proof_required:
  - "focused_test"
  - "broad_test"
evidence_budget: 0
raw_artifacts_allowed: false
machine_status: ""
human_status: ""
closeout_status: ""
agent_action: ""
next_owner: "none"
next_source: "status"
next_ref: ""
next_action: ""
domains: []
gates: []
dependencies:
  - "SRV-T-0007"
evidence_required: []
accepted_by: "human:sarav"
accepted_at: "2026-07-08T03:01:48Z"
closed_at: "2026-07-08T03:01:48Z"
created_at: "2026-07-06T16:45:00Z"
created_by: "agent:sarav"
updated_at: "2026-07-08T03:01:48Z"
updated_by: "human:sarav"
state_rev: "sha256:18e50341d9d37642261df87438251557a854f5372c1a6b5e1bc9be6ab677113b"
---

# SRV-T-0008 · Live updates: SSE event stream from the daemon poll loop

## Intent

Operator friction (2026-07-06): with the factory running live, parts of the UI update (queries on the 4s `LIVE_REFETCH_MS` poll) while others sit stale until a hard refresh — uneven refetch coverage, inherent to client polling. Replace discovery-by-polling with push: a `GET /api/stream` Server-Sent Events endpoint hosted by the daemon-embedded server (SRV-T-0007 — the poll loop is in the same process, so it is the natural event source). SSE, not WebSocket: the flow is strictly server→client for the read-only surface, SSE rides plain HTTP through the embedded single-binary server, and browser `EventSource` auto-reconnects across daemon restarts — a normal event in this system, not an edge case.

Event design: small invalidation hints, not data payloads — `{"kind": "<event>", "keys": ["runs", "tasks:AGX-T-0005", ...]}` — emitted on poll tick completion, dispatch, lease/liveness transition, task status change, and review-batch trigger (BACKEND-GAPS §6/§7). The client maps `keys` to TanStack Query keys and invalidates; the existing GET endpoints stay the single source of data. Include a heartbeat comment every ~15s so proxies and EventSource notice a dead daemon quickly. Client keeps a degraded 30–60s polling fallback when the stream is down, and the daemon-down state renders honestly (footer shows stream disconnected + last-event age, no fake green).

## Acceptance

| ID | Outcome | Proof |
|---|---|---|
| A1 | `GET /api/stream` emits SSE events for poll tick, dispatch, lease/liveness change, and task status change, each carrying invalidation keys; no data payloads duplicated. | Inline verification |
| A2 | The stream sends periodic heartbeats; a client whose stream drops falls back to slow polling and recovers automatically when the stream returns (EventSource reconnect). | Inline verification |
| A3 | Stream connections are read-only, localhost-bound, and shed cleanly on daemon shutdown without blocking the poll loop (bounded per-client buffers; a slow consumer is dropped, never backpressures dispatch). | Inline verification |
| A4 | UI: all live views update within one poll interval of a state change with no manual refresh, verified against a running daemon; polling interval code path remains as fallback only. | Inline verification |

## Non-goals

- No WebSocket, no bidirectional messaging, no auth (localhost read-only unchanged).
- No event payload as data source — GET endpoints remain canonical.
- No durable event log (TRC-T-0003 owns event-log-as-source-of-truth exploration).

## Verification

| Covers | Check | Result | Notes |
|---|---|---|---|
| A1 | go test ./cmd/tusker -run 'ServeStream' -count=1 | pass | 2 focused tests passed; stream frames use the default SSE message event compatible with EventSource.onmessage and carry only kind/keys. |
| A2 | go test ./cmd/tusker -run 'StreamHeartbeat\|StreamReconnect' -count=1 | pass | 2 focused tests passed: heartbeat comments and fresh subscription reconnect. |
| A3 | go test ./cmd/tusker -run 'StreamShutdown\|StreamSlowClient' -count=1 | pass | 1 focused test passed: shutdown closes clients and a full slow-client buffer is dropped without blocking broadcast. |
| A4 | bun --cwd internal/serve/ui test stream | pass | 2 UI tests passed: invalidation mapping, 45s disconnected fallback, and reconnect recovery. |
| A1,A2,A3,A4 | go build ./...; go vet ./...; go test ./... -count=1; bun --cwd internal/serve/ui build | pass | Serial broad validation passed: Go build, Go vet, full Go suite 439 tests, and production UI build. |

## Evidence

Accepted:
- None.

Pending:
- None.

## Knowledge delta

None expected.
