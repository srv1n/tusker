# Agent Feedback

- context: Current-only reset and fresh delivery import
- friction: The supported reset recreates factory codex_acp defaults even when the live catalog has no codex_acp bundle; a valid interactive route therefore needs a post-reset managed correction before task start.
- product-idea: Make reset initialize the registered current runner defaults or expose a supported current-runner selection step.
- impact: Fresh vaults appear configured but runner route is unavailable until corrected.
- related: reset, runner catalog, config resolve, interactive work start
- dedupe-key: reset-restores-unavailable-runner
