{
  "schema": "tusker.delivery-plan/v2",
  "scope": "compact-project-navigation-20260910",
  "title": "Compact project navigation with direct horizontal scrolling",
  "epic": "WUX",
  "spec_refs": [
    ".tusker/specs/work-area-redesign.md"
  ],
  "context_fingerprint": "sha256:dcc3526b5a7501bcee56bce78809661581ec546d56a3388b5c367f2adf51928b",
  "factory_intake_contract_schema": "tusker.factory-intake-contract/v1",
  "factory_intake_contract_version": "1.1.0",
  "factory_intake_contract_fingerprint": "sha256:15ec23480f22cb10b83bc945465abedd279e3954e777dcecb0815571799fbe18",
  "summary": "People can switch directly among projects in a compact scrolling strip, pin frequent projects, and use the full content width with clear icon-only settings scopes.",
  "concurrency": 1,
  "requirements": [
    {
      "id": "NAV1",
      "outcome": "Reclaim the permanent project sidebar with one horizontal strip containing every navigable project."
    },
    {
      "id": "NAV2",
      "outcome": "Use icon-only global search/settings and project settings, with one current project label and clear scope."
    },
    {
      "id": "NAV3",
      "outcome": "Persist pins and recent order while keeping mounted click targets stable."
    },
    {
      "id": "NAV4",
      "outcome": "Preserve project/checkout identity, route restoration, deep links and tolerant preference migration."
    },
    {
      "id": "NAV5",
      "outcome": "Support keyboard, mouse and touch overflow without page overflow or a mandatory project picker."
    },
    {
      "id": "NAV6",
      "outcome": "Preserve existing registration, refresh, search, attention, secondary routes, Documents and embedded behavior."
    },
    {
      "id": "NAV7",
      "outcome": "Qualify the actual integration, retain screenshot/interaction evidence, and update current system documentation."
    }
  ],
  "non_goals": [
    "Implementing other Work redesign requirements outside section 4.",
    "Changing backend, project identity, execution policy or installed runtimes.",
    "Starting a daemon, nested worker, automation or wave during planning."
  ],
  "assumptions": [
    {
      "source_key": "order-default",
      "statement": "Fresh-mount recency ordering with stable mounted positions is the reversible implementation default; it is not a separately quoted user requirement."
    },
    {
      "source_key": "existing-work",
      "statement": "Existing WUX-T-0003 and WUX-T-0009 are older held shell contracts. These tasks replace the navigation design only, without claiming to finish unrelated work."
    }
  ],
  "unresolved_decisions": [],
  "tasks": [
    {
      "source_key": "project-state",
      "title": "Preserve project pins, recent order and return locations",
      "outcome": "People can keep frequent projects at the front and return to their exact work without projects jumping around on each click. Done is a tested preference migration and a concise behavior report. Scope is saved navigation and restoration; visual shell changes belong to the next task.\n\n## Implementation notes\nRead .tusker/specs/work-area-redesign.md section 4 and its September 10 decision entry before editing. This replaces the old sidebar contract; other Work redesign tasks are not prerequisites. Inspect the current dirty checkout and all callers of changed helpers. Reuse existing code and preserve concurrent work. This task is inert planning until assigned; implement interactively through the supported Tusker claim protocol, without starting a daemon, nested worker or automation. Use configured Standard work and review levels. Keep tests focused and defer build until the final qualification task. Review acceptance against actual changed material; retain lifecycle/independent-review requirements without adding a human code-review gate. Do not mutate tracker records by hand.\n\nOwn internal/serve/ui/src/features/workbench/navigation/navigationState.ts and index.ts, test/project-strip-state.test.ts, relevant regressions in test/wux-navigation.test.ts, and docs/reports/project-strip/state.md. Extend the existing record with pinned IDs and recency. Preserve older records' routes and opaque view state, canonical/checkout identity, and safe URL handling. Prepare one session-stable ordered list on shell mount; explicit pin/order changes update it, ordinary switches update future recency only. Cross-project saved paths must never navigate to an unrelated project. Define the smallest state contract consumed by the shell; no new store, package or backend. Record implementation findings in state.md, including existing route defects fixed within this boundary. Add named tests with prefix project strip state; owned_paths below are authoritative repo-relative paths; abbreviated test paths in this paragraph are relative to internal/serve/ui.",
      "acceptance": [
        {
          "id": "A1",
          "outcome": "Migration preserves old last screens and opaque view state; blocked/malformed storage, duplicate/unknown IDs, renames and removals cannot break navigation."
        },
        {
          "id": "A2",
          "outcome": "Pinned IDs persist in stable order; fresh mounts order other projects by recency with deterministic ties; clicks within a mount do not reorder chips and same-project routes do not inflate recency."
        },
        {
          "id": "A3",
          "outcome": "Restoration honors deep links/back-forward and valid checkout ownership, rejects external or different-project saved routes, and uses the specified Work/Add project fallbacks."
        },
        {
          "id": "A4",
          "outcome": "A behavior matrix records actual focused checks covering migration, ordering and restoration, with no preview or installed-runtime claim."
        }
      ],
      "verification": [
        {
          "covers": "A1,A2,A3,A4",
          "check": "command: cd internal/serve/ui && bun test test/project-strip-state.test.ts",
          "notes": "Create the named test file with project strip state tests. No tests have been run for this planned implementation. Zero matched tests is a failure."
        }
      ],
      "dependencies": [],
      "artifact": {
        "kind": "behavior_matrix",
        "path": "docs/reports/project-strip/state.md",
        "summary": "Acceptance-linked migration, ordering and restoration results.",
        "acceptance_ids": [
          "A1",
          "A2",
          "A3",
          "A4"
        ]
      },
      "owned_paths": [
        "internal/serve/ui/src/features/workbench/navigation/navigationState.ts",
        "internal/serve/ui/src/features/workbench/navigation/index.ts",
        "internal/serve/ui/test/project-strip-state.test.ts",
        "internal/serve/ui/test/wux-navigation.test.ts",
        "docs/reports/project-strip/state.md"
      ],
      "work_level": "standard",
      "review_level": "standard",
      "complexity": "standard",
      "risk": "medium",
      "priority": "p1",
      "size": "m",
      "domains": [
        "project"
      ],
      "requirement_refs": [
        "NAV3",
        "NAV4"
      ]
    },
    {
      "source_key": "project-shell",
      "title": "Replace the project sidebar with a scrolling strip",
      "outcome": "People can scroll directly to any project, select it once, and use Work or Documents without repeated navigation branches. Global utilities and project settings use recognizable icons. Done is the actual application shell with before/after images and exercised interactions. Preserve current destinations and project identity; avoid redesigning page content.\n\n## Implementation notes\nRead .tusker/specs/work-area-redesign.md section 4 and its September 10 decision entry before editing. This replaces the old sidebar contract; other Work redesign tasks are not prerequisites. Inspect the current dirty checkout and all callers of changed helpers. Reuse existing code and preserve concurrent work. This task is inert planning until assigned; implement interactively through the supported Tusker claim protocol, without starting a daemon, nested worker or automation. Use configured Standard work and review levels. Keep tests focused and defer build until the final qualification task. Review acceptance against actual changed material; retain lifecycle/independent-review requirements without adding a human code-review gate. Do not mutate tracker records by hand.\n\nConsume project-state. Trace routes/__root.tsx and active WorkExperience before removing the desktop Sidebar. Reuse registration, refresh, search, project grouping/checkout selection, attention and existing menus. Proposed new ProjectStrip.tsx and ProjectStrip.css are file hints, not required abstractions. Keep all projects in one native horizontal scroller, nonwrapping, with overflow-only mouse arrows and focus reveal; no All projects menu or top-N cutoff. Remove visible Search/settings text and repeated project labels while preserving accessible names/tooltips. Wire one selected-project navigation row and its secondary actions; preserve global-scope and shell=1 behavior. Clean obsolete navigator/collapse paths and update their direct preview/test callers after checking usage, retaining Add project and recovery. Capture before-desktop.png before this task changes the shell. Add project strip shell focused tests and test/project-strip.browser.mjs against the real routed app, using existing Playwright loading patterns and the planned URL/env in spec section 4. Browser script must name cases project strip desktop, overflow, keyboard, persistence, scope, and regression; save the specified after screenshots and shell.md with fixture provenance. Tests must verify behavior and layout, not grep source strings. Independent review examines rendered evidence and route/state behavior; no screenshot score replaces acceptance.\n\nThe browser entry also provides --live using TUSKER_PROJECT_STRIP_LIVE_URL: no intercepted/fabricated API responses, no task execution or persisted settings changes, and explicit refusal if a real base URL is absent. Exercise read-only project switching, Work/Documents, both settings scopes, attention and back/forward; record actual routes. Isolate the browser context from the user's session.",
      "acceptance": [
        {
          "id": "A1",
          "outcome": "Before/after images show the permanent sidebar and width reservation removed, with one strip containing every project once and one Work/Documents row; no repeated project-name label."
        },
        {
          "id": "A2",
          "outcome": "Search, global settings and project settings are icon-only with accessible names/tooltips; their destinations and global versus project current states are correct."
        },
        {
          "id": "A3",
          "outcome": "Native scrolling, overflow-only arrows, keyboard focus reveal and pin/unpin/move actions work with 0, 1, 13 and 50 projects, long names and 1440/1024/390 widths; the page itself does not scroll horizontally."
        },
        {
          "id": "A4",
          "outcome": "Switching restores valid routes through the supplied state; no ordinary click reshuffles the mounted strip, lost focus or automatic snap-back while browsing other chips."
        },
        {
          "id": "A5",
          "outcome": "Add project, refresh, task search and shortcut, attention, meaningful checkout selection, secondary destinations, Documents tree and embedded panel remain reachable without changing execution authority."
        },
        {
          "id": "A6",
          "outcome": "The routed browser suite exercises named state/scope/regression scenarios, records matching before/after screenshots and reports fixture limitations without inventing live or installed proof."
        }
      ],
      "verification": [
        {
          "covers": "A1,A2,A3,A4,A5,A6",
          "check": "command: node internal/serve/ui/test/project-strip.browser.mjs",
          "notes": "New script to implement. Start Vite from internal/serve/ui on 127.0.0.1:5193 as specified; use an already-installed Playwright module. Fixture proof is labeled. Script must assert geometry and interactions and save images, not just HTTP success."
        },
        {
          "covers": "A2,A4,A5",
          "check": "command: cd internal/serve/ui && bun test test/project-strip-shell.test.ts",
          "notes": "Create project strip shell behavior cases, adapting existing router/test setup."
        },
        {
          "covers": "A1,A2,A3,A4,A5,A6",
          "check": "command: cd internal/serve/ui && bun run typecheck",
          "notes": "Supplemental static proof; report unrelated baseline failures without changing foreign code."
        }
      ],
      "dependencies": [
        {
          "task": "project-state",
          "kind": "hard"
        }
      ],
      "artifact": {
        "kind": "screenshot_set",
        "path": "docs/reports/project-strip/shell.md",
        "summary": "Before/after full-shell screenshots with browser interaction and regression results.",
        "acceptance_ids": [
          "A1",
          "A2",
          "A3",
          "A4",
          "A5",
          "A6"
        ]
      },
      "owned_paths": [
        "internal/serve/ui/src/routes/__root.tsx",
        "internal/serve/ui/src/router.tsx",
        "internal/serve/ui/src/components/Sidebar.tsx",
        "internal/serve/ui/src/features/workbench/navigation/ProjectStrip.tsx",
        "internal/serve/ui/src/features/workbench/navigation/ProjectStrip.css",
        "internal/serve/ui/src/features/workbench/navigation/ProjectNavigation.tsx",
        "internal/serve/ui/src/features/workbench/integration/WorkExperience.tsx",
        "internal/serve/ui/previews/wux/navigation",
        "internal/serve/ui/test/project-strip-shell.test.ts",
        "internal/serve/ui/test/project-strip.browser.mjs",
        "internal/serve/ui/test/navigation-simplification.test.ts",
        "internal/serve/ui/test/wux-integration.test.ts",
        "internal/serve/ui/tests/project-switching.test.ts",
        "docs/reports/project-strip/before-desktop.png",
        "docs/reports/project-strip/after-desktop.png",
        "docs/reports/project-strip/after-overflow.png",
        "docs/reports/project-strip/after-global-settings.png",
        "docs/reports/project-strip/after-project-settings.png",
        "docs/reports/project-strip/after-narrow.png",
        "docs/reports/project-strip/shell.md"
      ],
      "work_level": "standard",
      "review_level": "standard",
      "complexity": "standard",
      "risk": "medium",
      "priority": "p1",
      "size": "m",
      "domains": [
        "project"
      ],
      "requirement_refs": [
        "NAV1",
        "NAV2",
        "NAV3",
        "NAV4",
        "NAV5",
        "NAV6"
      ]
    },
    {
      "source_key": "project-qualification",
      "title": "Qualify the compact navigation and update the user reference",
      "outcome": "The new navigation is backed by a built application, a reproducible acceptance report and accurate user documentation. Done distinguishes exercised browser behavior, live reads and installed-app checks so the next person knows exactly what works. This task verifies and documents the integrated change; it does not redesign the shell or enable background work.\n\n## Implementation notes\nRead .tusker/specs/work-area-redesign.md section 4 and its September 10 decision entry before editing. This replaces the old sidebar contract; other Work redesign tasks are not prerequisites. Inspect the current dirty checkout and all callers of changed helpers. Reuse existing code and preserve concurrent work. This task is inert planning until assigned; implement interactively through the supported Tusker claim protocol, without starting a daemon, nested worker or automation. Use configured Standard work and review levels. Keep tests focused and defer build until the final qualification task. Review acceptance against actual changed material; retain lifecycle/independent-review requirements without adding a human code-review gate. Do not mutate tracker records by hand.\n\nAfter project-shell, run the focused state/shell suite, typecheck and build once from internal/serve/ui. Reuse the browser script and images rather than creating a second harness. Verify the built UI using bun run preview -- --host 127.0.0.1 --port 5194 --strictPort and run the browser entry with TUSKER_PROJECT_STRIP_BASE_URL=http://127.0.0.1:5194. Preserve unrelated dist edits and generate the tracked assets only from the agreed integrated source. Make read-only live checks against an already-running service if available: real project identity, routing, settings scope, attention and Documents reachability; do not register a real project or execute work to prove navigation. If a live or Mac installed surface is unavailable, record its exact limitation separately; do not call fixture proof live. Update docs/system/serve-ui.md as current behavior only after actual implementation, covering strip, pins/recency, scope, restoration, narrow scrolling and secondary destinations. Save docs/reports/project-strip/report.md with actual commands/results, screenshots, reviewer findings and unresolved runtime limits. Tests or typecheck failing in unrelated baseline files remain explicit rather than prompting broad fixes. Route defects to their owning implementation task for correction, then rerun only affected checks. No install/restart/daemon launch or new dependencies. Independent review is required by normal lifecycle, not a mandatory human approval.",
      "acceptance": [
        {
          "id": "A1",
          "outcome": "The implemented UI builds with current assets; the same browser acceptance scenarios pass against the built preview, or exact actionable failures remain open rather than being called complete."
        },
        {
          "id": "A2",
          "outcome": "The report links before/after images and every navigation requirement to observed results, distinguishing state tests, routed fixture browser, read-only live service and Mac installed-runtime evidence."
        },
        {
          "id": "A3",
          "outcome": "The current Serve UI reference describes the shipped strip, pin/recency policy, icon scopes, restoration and secondary actions; it does not claim unimplemented behavior or unsupported notification history."
        },
        {
          "id": "A4",
          "outcome": "No regression in project identity, existing navigation/registration/recovery, accessible controls or embedded panel is left unresolved; independent review and any true runtime blockers are recorded through normal lifecycle."
        }
      ],
      "verification": [
        {
          "covers": "A1,A4",
          "check": "command: cd internal/serve/ui && bun test test/project-strip-state.test.ts test/project-strip-shell.test.ts && bun run typecheck && bun run build",
          "notes": "Run after implementation settles; preserve exact baseline failures. Do not repeatedly rebuild without a new change."
        },
        {
          "covers": "A1,A2,A4",
          "check": "command: TUSKER_PROJECT_STRIP_BASE_URL=http://127.0.0.1:5194 node internal/serve/ui/test/project-strip.browser.mjs",
          "notes": "Serve built preview on port 5194 first. Same named browser cases; zero matched cases fails."
        },
        {
          "covers": "A2,A3",
          "check": "command: tusker docs check docs/system/serve-ui.md --json",
          "notes": "Also inspect report and docs against actual built behavior. Existing unrelated corpus failures remain separate."
        },
        {
          "covers": "A2,A4",
          "check": "command: node internal/serve/ui/test/project-strip.browser.mjs --live",
          "notes": "Set TUSKER_PROJECT_STRIP_LIVE_URL to a verified already-running service. This mode performs real read-only route checks with no fixture API interception or work execution. Missing live service is an explicit evidence blocker, never a fake human gate or fixture PASS."
        }
      ],
      "dependencies": [
        {
          "task": "project-shell",
          "kind": "hard"
        }
      ],
      "artifact": {
        "kind": "behavior_matrix",
        "path": "docs/reports/project-strip/report.md",
        "summary": "Built-browser acceptance matrix and honest live/installed evidence boundaries.",
        "acceptance_ids": [
          "A1",
          "A2",
          "A3",
          "A4"
        ]
      },
      "owned_paths": [
        "internal/serve/ui/dist",
        "docs/system/serve-ui.md",
        "docs/reports/project-strip/report.md"
      ],
      "work_level": "standard",
      "review_level": "standard",
      "complexity": "standard",
      "risk": "medium",
      "priority": "p1",
      "size": "m",
      "domains": [
        "project"
      ],
      "requirement_refs": [
        "NAV1",
        "NAV2",
        "NAV3",
        "NAV4",
        "NAV5",
        "NAV6",
        "NAV7"
      ],
      "generated_outputs": [
        "internal/serve/ui/dist"
      ]
    }
  ]
}
